# AGENTS.md

## 목적

이 저장소에서 기획, 요구사항, ERD, API, 테크스펙, 구현은 서로 분리된 문서가 아니라 같은 서비스 규칙을 표현하는 산출물이다.

에이전트와 개발자는 설계 및 구현 과정에서 발견한 변경사항을 코드나 설계 문서에만 반영하지 않는다. 기획과 설계가 서로 다른 내용을 말하지 않도록 `docs/요구사항정의서.md`를 중심으로 관련 산출물을 함께 갱신한다.

## 기준 문서

- 개발 관점의 기준 문서는 `docs/요구사항정의서.md`이다.
- 화면 동작과 사용자 흐름은 승인된 최신 Figma V3 화면설계서와 기능정의서를 기준으로 확인한다.
- ERD, API 명세, 테크스펙, 상태 모델과 구현은 요구사항정의서의 용어·비즈니스 규칙·요구사항 ID를 따라야 한다.
- `docs/legacy/` 문서는 참고 자료이며 현재 요구사항의 근거로 자동 승계하지 않는다.
- 문서 사이에 충돌이 있으면 임의로 한쪽을 무시하지 말고 충돌 지점과 영향을 명시하여 해결한다.

## 설계 작업 전 필수 확인

ERD, API, 상태 모델, 도메인 경계, 비동기 작업, 결제·생성권 흐름 또는 테크스펙을 설계하기 전에 반드시 다음을 수행한다.

1. `docs/요구사항정의서.md`의 관련 도메인 정의, 비즈니스 규칙, 기능·비기능 요구사항, 상태 모델, 예외 시나리오와 결정 필요 항목을 읽는다.
2. 관련 Figma 화면설계서 및 기능정의서에서 정상 흐름과 실패·빈 상태·경계값을 확인한다.
3. 이번 설계 결정이 해결하는 요구사항 ID를 식별한다.
4. 기존 ERD·API·테크스펙이 있다면 동일한 용어와 상태를 사용하는지 확인한다.
5. 확정되지 않은 정책을 확정된 사실처럼 가정하지 않는다. 구현 방향에 중대한 영향을 주면 결정 필요 항목으로 남기고 사용자 또는 팀의 결정을 요청한다.

## 기획과 설계의 양방향 동기화

### 기획이 변경된 경우

화면, 사용자 흐름, 정책 또는 기능정의가 변경되면 다음 순서로 반영한다.

1. `docs/요구사항정의서.md`의 관련 도메인 책임과 비즈니스 규칙을 수정한다.
2. 기능 요구사항·비기능 요구사항·상태 모델·정상/예외 시나리오·결정 항목·추적성 표에서 영향을 받는 부분을 함께 수정한다.
3. 변경된 요구사항을 기준으로 ERD, API, 테크스펙과 구현을 수정한다.
4. 더 이상 유효하지 않은 규칙은 조용히 삭제하지 말고, 참조 문서가 남아 있다면 폐기 또는 대체 관계를 명확히 한다.

### 설계 또는 구현 과정에서 기획 변경이 필요한 경우

개발자가 데이터 정합성, 외부 API 제약, 보안, 성능, 실패 복구 또는 구현 가능성 때문에 기존 기획을 변경해야 한다고 판단하면 다음을 수행한다.

1. 기존 기획과 충돌하는 지점을 구체적으로 설명한다.
2. 선택한 설계, 고려한 대안, 선택 이유와 감수할 트레이드오프를 기록한다.
3. `docs/요구사항정의서.md`의 비즈니스 규칙과 기능·비기능 요구사항을 실제 결정에 맞게 수정한다.
4. 화면 문구·버튼 동작·사용자 흐름·오류 처리에 영향이 있으면 Figma 화면설계서와 기능정의서의 수정 필요 사항도 함께 정리한다.
5. 외부 Figma나 기능정의서를 직접 수정할 권한 또는 도구가 없다면 요구사항정의서에 동기화 필요 사항을 남기고, 최종 응답에서 사용자에게 미반영된 기획 산출물을 명확히 알린다.
6. 기획 변경이 확정되기 전에는 코드만 먼저 확정된 동작으로 구현하지 않는다. 단, 되돌릴 수 있는 초안이나 PoC는 가정임을 명시한다.

## 요구사항정의서 수정 원칙

- 변경된 사실을 관련된 기존 섹션에 반영한다. 문서 끝에 메모만 추가하여 본문과 상충하게 만들지 않는다.
- 기존 요구사항 ID의 의미가 유지되는 수정은 같은 ID를 사용한다.
- 의미가 달라지거나 새로운 규칙이 생기면 새 ID를 부여한다.
- 이미 다른 산출물에서 참조하는 ID를 재사용하여 다른 의미로 바꾸지 않는다.
- 확정되지 않은 값은 `결정 필요`로 표시하고 결정 항목에 등록한다.
- 도메인 명칭, 상태명, 필드명과 오류 코드는 ERD·API·테크스펙 전반에서 동일하게 사용한다.
- 화면에만 존재하는 표시값과 DB에 영속화할 원본 데이터를 구분한다.
- 성공 흐름뿐 아니라 중복 요청, 권한 오류, 결과 없음, 만료, 외부 연동 실패, 재시도와 복구 규칙까지 확인한다.

## 변경 시 함께 확인할 섹션

비즈니스 동작이 바뀌면 최소한 다음 항목의 영향 여부를 확인한다.

- 도메인 정의와 책임 경계
- 용어 사전
- 사용자 시나리오
- 비즈니스 규칙(BR)
- 기능 요구사항(FR)
- 비기능 요구사항(NFR)
- 주요 상태 모델
- 정상·예외·경계 시나리오
- ERD 테이블 후보와 제약
- API 경계와 비동기 처리 방식
- 기획·프론트엔드 결정 필요 항목
- 요구사항 추적성
- 현재 결론

영향이 없는 항목은 억지로 수정하지 않되, 영향 검토 자체는 생략하지 않는다.

## 주요 설계 변경 예시

예를 들어 가이드북 재생성이 “새 가이드북 생성”에서 “기존 가이드북 갱신”으로 변경되면 테이블 컬럼만 수정해서는 안 된다. 다음을 함께 확인한다.

- 재생성 비즈니스 규칙과 생성권 사용 규칙
- `GenerationJob`과 `Guidebook`의 관계 및 상태 모델
- 재생성 API의 작업 ID·가이드북 ID 의미
- 실패 시 기존 가이드북 보존 규칙
- 일정 교체와 트랜잭션 범위
- 공유 링크, PDF, 평가 대상과 가져온 복사본에 미치는 영향
- 화면에서 재생성 결과를 새 카드로 보여줄지 기존 카드에 반영할지

## ERD 및 API 설계 규칙

- DTO 필드를 그대로 테이블 컬럼으로 옮기지 않는다. 영속 원본, 계산 가능한 응답값, 화면 표시값과 외부 시스템 식별자를 구분한다.
- 테이블을 추가할 때는 반복 행, 독립 생명주기, 참조 관계, 이력 또는 유일 제약 중 어떤 근거가 있는지 설명한다.
- 테이블을 병합하거나 생략할 때는 얻는 단순성과 잃는 정합성·이력·확장성을 기록한다.
- API가 요구사항과 다른 생명주기를 표현하면 백엔드 매핑으로 숨길 수 있는지 먼저 판단하되, 계약 의미가 모호하거나 양쪽 구현이 달라질 수 있으면 API 명세도 수정한다.
- 외부 API와 내부 DB의 ID, 상태, 오류 코드는 같은 개념인지 확인하고 다르면 명시적으로 매핑한다.
- 스키마나 API 계약 변경이 발생하면 요구사항정의서와 관련 설계 문서를 같은 작업에서 갱신한다.

## 완료 전 셀프 리뷰

설계 또는 구현 작업을 완료하기 전에 다음을 확인한다.

- [ ] 작업이 해결하는 요구사항 ID를 설명할 수 있다.
- [ ] 변경된 기획 또는 설계 내용이 `docs/요구사항정의서.md`에 반영되어 있다.
- [ ] 기획 변경이 필요한 경우 화면설계서·기능정의서 수정 필요 사항을 정리했다.
- [ ] ERD, API, 상태 모델과 코드가 같은 용어와 상태를 사용한다.
- [ ] 정상 흐름과 실패·중복·경계·복구 흐름을 함께 검토했다.
- [ ] 선택한 설계의 대안과 트레이드오프를 설명할 수 있다.
- [ ] 문서에 남은 이전 정책이나 모순된 설명을 검색했다.
- [ ] 외부 문서에 아직 반영하지 못한 사항을 최종 응답에서 명확히 알렸다.

## 예외

오탈자, 서식 변경, 변수명 정리처럼 사용자 동작·비즈니스 규칙·데이터 계약에 영향을 주지 않는 변경은 요구사항정의서 갱신 대상이 아니다.

반대로 화면에 드러나지 않더라도 다음 변경은 요구사항정의서 또는 관련 설계 근거를 갱신해야 한다.

- 데이터 소유권 또는 도메인 경계 변경
- 상태 전이와 생명주기 변경
- API 요청·응답 의미 변경
- DB 무결성·유일 제약 변경
- 생성권·결제·평가처럼 사용자 권리나 원장에 영향을 주는 변경
- 재시도·멱등성·실패 보상 방식 변경
- 개인정보 보존·삭제와 인증 정책 변경
