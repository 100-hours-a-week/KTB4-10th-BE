# KGB Backend 기여 가이드

이 문서는 KGB 백엔드 저장소에서 코드를 변경하고 리뷰하는 공통 절차를 정의한다. 애플리케이션 구조, 도메인 경계와 버전별 구현 방식은 이 문서에서 확정하지 않고 해당 버전의 설계 문서와 코드 구성을 따른다.

## 1. 기본 원칙

- Java, Spring Boot와 Gradle 버전은 Gradle 빌드 파일과 Wrapper에 고정된 값을 기준으로 한다.
- Gradle 설정은 Kotlin DSL을 사용한다.
- 하나의 작업은 하나의 주요 목적을 가진다.
- 기능 구현자는 구현에 필요한 테스트와 관련 문서를 함께 관리한다.
- 확정되지 않은 정책은 임의로 구현하지 않고 Issue 또는 관련 설계 문서에서 합의한다.
- 비밀키, 비밀번호, 토큰과 개인정보를 저장소나 로그에 남기지 않는다.
- `docs/legacy/` 문서는 참고 자료이며 현재 요구사항으로 자동 승계하지 않는다.

## 2. 브랜치 전략

브랜치는 `main → dev → 작업 브랜치` 구조로 운영한다.

| 브랜치 | 용도 |
| --- | --- |
| `main` | 검증이 완료된 안정 버전 |
| `dev` | 다음 버전 개발 내용을 통합하는 브랜치 |
| `feat/*` | 기능 개발 |
| `fix/*` | 개발 중 발견한 버그 수정 |
| `refactor/*` | 동작을 바꾸지 않는 구조 개선 |
| `test/*` | 테스트 추가 및 개선 |
| `docs/*` | 문서 변경 |
| `chore/*` | 빌드, 설정과 의존성 관리 |
| `ci/*` | CI 설정 변경 |
| `hotfix/*` | `main`에서 발견된 긴급 오류 수정 |

### 2.1 작업 흐름

1. 작업할 내용의 Issue를 생성한다.
2. 최신 `dev`에서 작업 브랜치를 만든다.
3. 구현, 테스트와 필요한 문서 수정을 진행한다.
4. 작업 방향을 일찍 공유할 필요가 있으면 Draft PR을 생성한다.
5. 로컬에서 `./gradlew clean check`를 실행한다.
6. 작업 브랜치에서 `dev`를 대상으로 Pull Request를 생성한다.
7. 상대 팀원에게 리뷰를 요청한다.
8. 승인 1개와 필수 CI 통과를 확인한다.
9. Squash Merge로 `dev`에 병합하고 작업 브랜치를 삭제한다.
10. 검증된 기능 묶음은 `dev → main` PR로 병합한다.

`main`과 `dev`에는 직접 Push하거나 Force Push하지 않는다.

### 2.2 브랜치 이름

```text
<type>/<issue-number>-<short-description>
```

예시:

```text
feat/12-oauth-login
feat/18-guidebook-generation
fix/24-duplicate-credit-consumption
refactor/31-generation-service
test/35-concurrency-test
chore/2-configure-static-analysis
ci/3-add-pr-check
```

- 영문 소문자를 사용한다.
- 단어는 하이픈으로 구분한다.
- Issue 번호를 포함한다.
- 개발자 이름은 포함하지 않는다.
- 하나의 브랜치는 하나의 주요 목적만 가진다.

### 2.3 Hotfix

`main`의 긴급 문제는 `main`에서 `hotfix/*` 브랜치를 생성한다. 수정 PR을 `main`에 병합한 후 동일한 변경을 `dev`에도 즉시 반영한다.

## 3. Issue 규칙

Issue 제목은 다음 형식을 사용한다.

```text
[영역] 구현할 기능
[버그][영역] 발생한 문제
```

예시:

```text
[인증] OAuth 로그인 기능 구현
[가이드북] 생성 작업 접수 API 구현
[공통] 정적 분석 도구 설정
[버그][생성권] 동시 요청에서 잔액이 중복 차감되는 문제
```

Issue 하나는 가능하면 한 사람이 1~3일 안에 하나의 PR로 완료할 수 있는 크기로 나눈다. 큰 작업은 상위 Issue를 만들고 기능 단위 하위 Issue를 연결한다.

### 3.1 기능 Issue 본문

```markdown
## 작업 목적

이 작업이 필요한 이유를 작성합니다.

## 관련 자료

- 요구사항:
- API 문서:
- 설계 문서:

## 작업 범위

- [ ]
- [ ]
- [ ]

## 완료 조건

- [ ] 정상 흐름을 구현한다.
- [ ] 필요한 실패·경계 조건을 처리한다.
- [ ] 필요한 테스트를 작성한다.
- [ ] 관련 문서를 갱신한다.

## 고려할 예외

- 인증·권한:
- 결과 없음:
- 중복 요청:
- 외부 연동 실패:
- 동시 요청:

## 영향 범위

- API:
- 데이터:
- 관련 기능:
- 문서:
```

### 3.2 버그 Issue 본문

```markdown
## 문제

현재 발생하는 문제를 작성합니다.

## 재현 방법

1.
2.
3.

## 기대 동작

정상적으로 기대하는 결과를 작성합니다.

## 실제 동작

실제로 발생한 결과를 작성합니다.

## 영향 범위

- 사용자 영향:
- 데이터 영향:
- 관련 기능:

## 완료 조건

- [ ] 문제를 재현하는 테스트를 작성한다.
- [ ] 수정 전 테스트 실패를 확인한다.
- [ ] 문제를 수정한다.
- [ ] 수정 후 테스트 성공을 확인한다.
- [ ] 유사한 경로에 같은 문제가 없는지 확인한다.
```

## 4. 커밋 규칙

커밋 메시지는 다음 형식을 사용한다.

```text
<type>(<scope>): <한글 설명>
```

`scope`는 선택 사항이며 변경된 기능 또는 영역을 짧게 표시한다.

| Type | 의미 |
| --- | --- |
| `feat` | 새로운 기능 |
| `fix` | 버그 수정 |
| `refactor` | 기능 변경 없는 구조 개선 |
| `test` | 테스트 추가 및 수정 |
| `docs` | 문서 변경 |
| `chore` | 설정, 빌드와 의존성 변경 |
| `ci` | CI 변경 |
| `perf` | 성능 개선 |
| `style` | 코드 포맷 변경 |

예시:

```text
feat(auth): OAuth 로그인 기능 추가
feat(guidebook): 생성 작업 접수 기능 추가
fix(payment): 생성권 중복 차감 방지
test(guidebook): 생성 요청 멱등성 테스트 추가
chore(build): Checkstyle과 SpotBugs 설정
docs(api): 가이드북 생성 API 명세 수정
```

중요한 설계 결정이 있으면 본문에 변경 이유를 작성한다.

```text
feat(guidebook): 생성 작업 접수 기능 추가

장시간 작업을 요청 처리와 분리하기 위해 작업을 먼저 저장한다.
후속 처리의 실패가 접수 요청을 중복 생성하지 않도록 멱등 키를 사용한다.

Refs: #18
```

`작업 중`, `수정`, `최종`처럼 변경 목적을 알 수 없는 메시지는 사용하지 않는다.

## 5. Pull Request 규칙

PR 제목은 Squash Merge 후 최종 커밋 메시지로 사용할 수 있도록 커밋 메시지와 동일한 형식으로 작성한다.

```text
feat(guidebook): 가이드북 생성 작업 접수 기능 추가
fix(payment): 생성권 중복 차감 문제 수정
chore(build): Checkstyle과 SpotBugs 적용
```

- 하나의 PR은 하나의 Issue 또는 하나의 주요 변경 목적을 해결한다.
- 기능과 무관한 리팩터링을 같은 PR에 섞지 않는다.
- 데이터 구조 변경과 이를 사용하는 코드는 함께 제출한다.
- API나 사용자 동작이 변경되면 관련 문서를 함께 수정한다.
- PR을 병합하려면 상대 팀원 승인 1개와 필수 CI 통과가 필요하다.
- 일반 PR은 `dev`를 대상으로 하고 Squash Merge한다.

### 5.1 PR 본문

````markdown
## 변경 내용

-
-

## 관련 Issue 및 자료

- Close #
- 요구사항:
- 설계 문서:

## 구현 결정

중요한 구현 방식과 선택 이유를 작성합니다.

## 테스트

- [ ] 단위 테스트
- [ ] 애플리케이션 테스트
- [ ] 데이터 접근 통합 테스트
- [ ] API 테스트
- [ ] 동시성 테스트
- [ ] 해당 없음

## 테스트 결과

```text
./gradlew clean check
```

## 영향 범위

- API:
- 데이터 변경:
- 관련 기능:
- 문서:

## 리뷰 포인트

리뷰어가 집중해서 확인할 부분을 작성합니다.

## 체크리스트

- [ ] 최신 `dev`를 기준으로 작업했습니다.
- [ ] `./gradlew clean check`가 성공합니다.
- [ ] 정상·실패·경계 조건을 검토했습니다.
- [ ] Checkstyle 예외를 불필요하게 추가하지 않았습니다.
- [ ] SpotBugs 경고를 근거 없이 제외하지 않았습니다.
- [ ] 변경된 계약과 동작을 관련 문서에 반영했습니다.
- [ ] 비밀정보나 개인정보를 로그에 남기지 않았습니다.
````

### 5.2 리뷰 의견 표시

리뷰 의견 앞에 필요하면 다음 접두사를 사용한다.

| 접두사 | 의미 |
| --- | --- |
| `[BLOCK]` | 병합 전에 반드시 수정 필요 |
| `[SUGGEST]` | 개선 제안, 반영 여부 논의 가능 |
| `[QUESTION]` | 구현 의도 또는 근거 확인 |
| `[NIT]` | 사소한 이름이나 표현 제안 |

리뷰어는 최소한 다음을 확인한다.

- Issue의 완료 조건과 실제 동작이 일치하는가?
- 변경 범위와 책임이 과도하게 섞이지 않았는가?
- 인증, 권한, 중복 및 동시 요청을 필요한 수준으로 처리하는가?
- 실패 시 데이터가 중간 상태로 남지 않는가?
- 테스트가 구현 세부사항이 아닌 외부 동작과 규칙을 검증하는가?
- 변경된 API, 데이터와 사용자 동작이 관련 문서에 반영됐는가?

## 6. 정적 분석

정적 분석과 테스트는 `./gradlew clean check`로 실행한다.

### 6.1 Checkstyle

Checkstyle은 운영 코드와 테스트 코드에 모두 적용한다.

```text
src/main/java → checkstyleMain
src/test/java → checkstyleTest
```

초기 필수 규칙은 다음과 같다.

- 와일드카드 및 사용하지 않는 Import 금지
- 들여쓰기 4칸, 탭 문자 금지
- 한 줄 최대 120자
- 중괄호 사용
- 클래스, 메서드, 변수와 상수 이름 규칙
- Modifier 순서
- 빈 블록 금지
- 파일 마지막 개행

모든 클래스와 메서드의 Javadoc 작성처럼 의미 없는 문서를 양산하는 규칙은 초기 필수 규칙으로 사용하지 않는다.

### 6.2 SpotBugs

초기에는 운영 코드인 `spotbugsMain`을 필수 검사하고 `spotbugsTest`는 제외한다. 테스트 코드에서 실제로 유용한 결과를 얻을 수 있다고 판단되면 검사 범위를 확장한다.

- 분석 수준은 `Effort.MAX`를 사용한다.
- 신뢰도는 `Confidence.MEDIUM` 이상을 PR 차단 대상으로 한다.
- 경고가 발생하면 Suppression보다 코드 수정을 우선한다.
- 오탐으로 확인되면 가장 좁은 클래스와 Bug Pattern만 제외한다.
- 제외 사유를 설정 파일의 주석과 PR 본문에 기록한다.
- 패키지 전체를 정적 분석에서 제외하지 않는다.

## 7. 테스트 규칙

기능 구현자가 해당 기능의 테스트까지 작성한다. 별도의 테스트 담당자에게 구현 이후 테스트를 넘기지 않는다.

### 7.1 테스트 범위

| 종류 | 주요 검증 대상 |
| --- | --- |
| 단위 테스트 | 계산, 상태 전이, 유효성 검사와 경계값 |
| 애플리케이션 테스트 | 기능 흐름과 협력 객체의 상호작용 |
| 데이터 접근 통합 테스트 | 제약, 조회, 저장, 잠금과 마이그레이션 |
| API 테스트 | 입력 검증, 인증·인가, 상태 코드와 직렬화 |
| 통합·동시성 테스트 | 여러 컴포넌트가 연결된 핵심 성공·실패 흐름 |

테스트 도구와 데이터베이스 종류는 현재 버전의 빌드 설정 및 설계 문서를 따른다.

### 7.2 테스트 이름

테스트 메서드명은 영어로 작성하고 `@DisplayName`에는 기대 동작을 한글로 작성한다.

```java
@Test
@DisplayName("동일한 요청이 반복되면 기존 결과를 반환한다")
void returnsExistingResultWhenRequestIsDuplicated() {
    // given

    // when

    // then
}
```

- `test1`, `successTest`처럼 의미가 모호한 이름은 사용하지 않는다.
- Given-When-Then 구조를 기본으로 사용하되 단순한 테스트에 불필요한 빈 구획을 강제하지 않는다.
- 한 테스트는 하나의 핵심 동작 또는 실패 원인을 검증한다.
- 현재 시간, 실행 순서나 무작위 값에 의존하지 않는다.

### 7.3 테스트 Fixture

반복되는 유효한 테스트 객체는 기능 또는 테스트 대상별 Fixture로 만든다.

Fixture 규칙은 다음과 같다.

- Fixture는 테스트 소스에만 둔다.
- 기본 Fixture는 항상 유효한 객체를 반환한다.
- 테스트의 핵심 조건은 Fixture에 숨기지 않고 테스트에서 명시한다.
- 무작위 값보다 고정되고 재현 가능한 값을 사용한다.
- Fixture 객체를 테스트 간 공유하거나 변경하지 않는다.
- 값 조합이 많아질 때만 Test Data Builder를 도입한다.

## 8. 완료 기준

다음 조건을 모두 만족해야 Issue를 완료할 수 있다.

- [ ] Issue의 완료 조건을 구현했다.
- [ ] 필요한 성공·실패·경계 테스트를 작성했다.
- [ ] `./gradlew clean check`가 성공한다.
- [ ] 상대 팀원 리뷰 승인을 받았다.
- [ ] 변경된 API, 데이터 또는 사용자 동작을 관련 문서에 반영했다.
- [ ] 요구사항이 변경됐다면 기준 요구사항 문서와 관련 설계 문서를 갱신했다.
- [ ] PR을 Squash Merge로 `dev`에 병합했다.
