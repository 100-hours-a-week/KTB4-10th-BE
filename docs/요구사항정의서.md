# **V3 요구사항정의서**

## **0단계 프로젝트 요구사항 분석**

> 이 문서는 V3 화면설계서와 기능정의서를 개발 관점에서 해석한 설계 초안이다.
> 
> 
> 확정되지 않은 내용은 **결정 필요**로 표시하며, 이후 ERD·API·테크스펙은 본 문서의 용어와 요구사항 ID를 기준으로 연결한다.
> 

### **1. 문서 목적**

V3 서비스의 업무 영역을 6개 도메인으로 구분하고 각 도메인의 책임과 데이터 소유권을 정한다.

화면 단위 설명을 개발 가능한 비즈니스 규칙과 기능·비기능 요구사항으로 변환한다.

ERD, API, 배치 작업, 외부 연동 설계가 어떤 요구사항에서 시작됐는지 추적할 수 있게 한다.

정상 동작뿐 아니라 중복 요청, 실패, 경계값, 복구가 필요한 상황을 미리 정의한다.

PL 미팅에서 “왜 이렇게 나눴는가”와 “어떤 선택을 했는가”를 설명할 근거로 사용한다.

### **2. 분석 범위와 전제**

#### **2.1 기준 범위**

Figma의 **V3 프레임 내부 화면**을 우선 기준으로 한다.

V1·V2는 V3에서 의미가 불명확할 때 참고만 하며, V3 요구사항으로 자동 승계하지 않는다.

화면에 보인다는 이유만으로 별도 도메인이나 영속 엔티티로 만들지 않는다.

결제처럼 V3에 정책 화면만 있더라도 생성권과 직접 연결된 핵심 업무는 설계 범위에 포함한다.

#### **2.2 도메인 분리 기준**

도메인은 화면이나 메뉴가 아니라 다음 기준으로 나눈다.

함께 변경되는 비즈니스 규칙과 데이터인가?

해당 데이터의 생성·변경·삭제 권한을 한 책임 주체가 설명할 수 있는가?

독립적인 상태 전이와 정합성 규칙이 있는가?

다른 영역이 원본을 수정하는 대신 식별자로 참조할 수 있는가?

현재 팀 규모에서 별도 모듈로 나누는 실익이 복잡도보다 큰가?

#### **2.3 병합 결정과 트레이드오프**

| **병합 결정** | **선택 근거** | **감수할 단점** |
| --- | --- | --- |
| 취향 → 회원 | V3의 취향은 여행별 별도 객체가 아니라 회원의 현재 기본 취향으로 사용됨 | 추천 기능이 커지면 취향 모듈을 다시 분리해야 할 수 있음 |
| 서비스 설정·정책 → 관련 도메인 | 언어·약관 동의는 회원, 푸시는 알림, 결제 정책은 결제가 소유하는 편이 책임이 명확함 | 정책 조회를 한 화면에서 조합하는 애플리케이션 계층이 필요함 |
| 탐색·지도 → 관광 콘텐츠 | 검색·필터·지도는 관광 콘텐츠의 대표 조회 방식이며 원본을 소유하지 않음 | 검색 인덱스가 커지면 별도 조회 모듈로 분리할 가능성이 있음 |
| 공유·가져오기 → 가이드북 | 공유 대상과 복제 결과가 모두 가이드북이며 독립 상품이 아님 | 공유 정책이 복잡해지면 가이드북 모듈이 커질 수 있음 |
| 평가·랭킹 유지 | 평가 원본의 무결성과 랭킹 집계·보정 규칙이 독립적임 | 관광 콘텐츠 조회 시 랭킹 데이터를 결합해야 함 |
| 알림 유지 | 읽음 상태, 푸시 수신 설정, 재시도 등 독립적인 전달 생명주기가 있음 | 여러 도메인의 사건을 구독하는 연결 지점이 됨 |
| 생성권·결제 유지 | 금액·결제·환불·잔액은 강한 정합성과 감사 이력이 필요함 | 외부 PG 연동과 생성권 예약·웹훅 재처리로 구현 복잡도가 큼 |

### **3. 도메인 개요**

| **ID** | **최종 도메인** | **병합 대상** | **핵심 책임** |
| --- | --- | --- | --- |
| DOM-01 | 회원 | 회원·인증 + 취향 + 서비스 설정·정책 일부 | OAuth, 회원, 프로필, 세션, 기본 취향, 언어 설정, 약관 동의 |
| DOM-02 | 관광 콘텐츠·탐색 | 관광 콘텐츠 + 탐색·지도 | 장소·행사 원본, 검색, 필터, 현재 위치, 지도, 마커, 관심 장소 |
| DOM-03 | 가이드북 | 가이드북·일정 + 공유·가져오기 | 생성 조건, AI 생성, 일정, 공유 링크, 가져오기, HTML/PDF |
| DOM-04 | 평가·랭킹 | 기존 유지 | 여행 종료 후 평가, 별점, 집계, 일간·주간·월간 랭킹 |
| DOM-05 | 알림 | 기존 유지 | 인앱 알림, 읽음 상태, 푸시 수신 설정 |
| DOM-06 | 생성권·결제 | 기존 유지 | 생성권, 상품, 주문, 결제, 환불, 잔액 |

### **4. 도메인 상세 정의**

#### **4.1 DOM-01 회원**

**정의**

서비스를 사용하는 사람을 식별하고, 로그인 상태와 회원에게 지속적으로 귀속되는 기본 설정을 관리하는 영역이다.

**핵심 책임**

소셜 OAuth 인증과 외부 계정 연결

회원 생성, 조회, 프로필 수정, 로그아웃, 탈퇴

액세스·리프레시 세션의 발급, 갱신, 폐기

최초 취향 선택과 마이페이지의 기본 취향 조회·수정

표시 언어 저장

이용약관·개인정보 처리방침의 버전과 회원 동의 이력 관리

**소유 데이터**

Member, SocialAccount, Session

PreferenceProfile, PreferenceOption, PreferenceSelection

MemberSetting, PolicyDocument, MemberConsent

**경계**

관광 카테고리 원본은 DOM-02가 소유하며, 회원은 선택한 카테고리 식별자만 참조한다.

푸시 수신 설정은 DOM-05가, 결제·환불 동의 및 정책은 DOM-06이 소유한다.

생성권 잔액은 회원 속성이 아니라 DOM-06의 원장으로 관리한다.

#### **4.2 DOM-02 관광 콘텐츠·탐색**

**정의**

장소·행사·문화유산·명소의 기준 정보를 보유하고, 사용자가 검색·필터·지도·랭킹 화면에서 이를 찾도록 제공하는 영역이다.

**핵심 책임**

외부 관광 데이터 수집·정규화·갱신

장소 및 행사의 명칭, 위치, 기간, 운영 정보, 이미지, 출처 관리

키워드 검색, 지역·월·카테고리 필터, 페이지네이션

현재 위치 및 지도 범위 기반 조회

마커·클러스터 표시용 조회 모델 제공

회원의 관심 장소 등록·해제

DOM-04가 만든 랭킹 결과와 관광 콘텐츠 상세정보 결합

**소유 데이터**

TourismContent, EventDetail, ContentCategory

ContentImage, ContentSource, Location

FavoriteContent

**영속 데이터가 아닌 조회 모델**

SearchCondition, MapViewport, Marker, Cluster, SearchResult

**경계**

관광 콘텐츠의 원본 정보는 이 도메인만 변경한다.

가이드북 일정은 콘텐츠 ID를 참조하되 제목·위치 등 원본을 임의 변경하지 않는다.

별점 원본과 랭킹 순위는 DOM-04가 소유한다.

#### **4.3 DOM-03 가이드북**

**정의**

회원의 여행 조건과 기본 취향을 이용해 가이드북을 생성하고, 일정을 구성·수정·저장·공유·가져오기·내보내기 하는 영역이다.

**핵심 책임**

여행 지역, 기간, 인원 등 생성 조건 검증

DOM-01의 현재 기본 취향 조회

생성권 예약 요청과 AI 생성 작업을 관리하고, 생성 성공 시 사용 확정, 실패·취소 시 예약 해제를 요청한다.

기존 가이드북을 원본으로 한 재생성 작업과 생성 계보 관리

일자별 일정과 방문 순서 저장·수정

관광 콘텐츠와 일정 항목 연결

공유 링크 발급·만료·미리보기

다른 회원의 가이드북 가져오기와 중복 처리

HTML 보기 및 PDF 생성·다운로드

**소유 데이터**

Guidebook, TripCondition, GenerationJob

ItineraryDay, ItineraryItem

ShareLink, GuidebookImport, GuidebookExport

**경계**

콘텐츠 원본은 DOM-02를 참조한다.

기본 취향 원본은 DOM-01을 조회한다.

생성권의 예약·사용 확정·예약 해제는 DOM-06에 요청한다. DOM-03은 생성권 잔액을 직접 변경하지 않는다.

여행 종료 후 평가 원본은 DOM-04가 관리한다.

#### **4.4 DOM-04 평가·랭킹**

**정의**

종료된 여행의 일정에 포함된 장소에 대해 회원 평가를 수집하고, 평가 데이터를 보정·집계하여 일간·주간·월간 순위를 제공하는 영역이다.

**핵심 책임**

여행 종료 후 평가 가능 여부 판단

평가 안내 연기, 장소별 건너뛰기, 가이드북을 통한 재진입 상태 관리

방문 장소별 별점 입력·수정·제출

중복 제출 방지와 평가 원본 보존

일간·주간·월간 집계 구간 생성

베이지안 가중 평점 계산과 랭킹 스냅샷 제공

**소유 데이터**

RatingSession, PlaceRating

RankingSnapshot, RankingEntry

**랭킹 산정**

표본이 적은 장소가 소수의 높은 별점만으로 상위에 오르는 현상을 줄이기 위해 다음 베이지안 가중 평균을 사용한다.

가중 평점 = (v / (v + m)) × R + (m / (v + m)) × C

R: 해당 장소 평균 평점

v: 해당 장소의 유효 평가 수

C: 같은 집계 대상 전체의 평균 평점

m: 신뢰 가능한 최소 평가 수

m 값과 동점 처리 기준은 운영 데이터 확인 후 확정한다. 랭킹 기간은 **일간·주간·월간을 제공**한다.

**경계**

별점은 이 도메인의 원본 데이터다.

랭킹은 별점 원본에서 계산된 파생 결과이며 관광 콘텐츠 원본을 수정하지 않는다.

평가 대상은 DOM-03 일정의 방문 장소와 DOM-02 관광 콘텐츠를 참조한다.

#### **4.5 DOM-05 알림**

**정의**

다른 도메인에서 발생한 사용자 대상 사건을 인앱 알림과 푸시로 전달하고 읽음 상태를 관리하는 영역이다.

**핵심 책임**

평가 요청, 공유·가져오기, 가이드북 생성 완료 등 알림 생성

알림 목록 조회와 읽음·모두 읽음 처리

푸시 수신 설정 저장

중복 알림 방지, 푸시 실패 기록과 재시도

**소유 데이터**

Notification, PushPreference

**경계**

알림은 다른 도메인의 성공 여부를 결정하지 않는다.

원본 업무가 성공한 뒤 발생한 사건을 받아 알림을 만든다.

알림 삭제나 읽음 처리가 가이드북·평가 등 원본 데이터에 영향을 주지 않는다.

#### **4.6 DOM-06 생성권·결제**

**정의**

가이드북 생성에 필요한 사용 권한을 수량으로 관리하고, 상품 주문·결제·환불 결과에 따라 잔액을 일관되게 변경하는 영역이다.

**핵심 책임**

무료·유료 생성권 지급과 잔액 조회

상품, 주문, 결제 시도, 결제 완료 관리

결제 검증과 웹훅 중복 처리

환불 가능 여부 판단과 환불 이력 관리

가이드북 생성 전 사용 가능한 생성권 예약

생성 성공 시 예약된 생성권 사용 확정

생성 실패·취소 시 예약 해제

모든 증감 사유를 원장으로 보존

**소유 데이터**

Product, Order, PaymentAttempt, Refund

EntitlementLedger

**경계**

가이드북은 생성권 잔액을 직접 수정하지 않고 예약·사용 확정·예약 해제만 요청한다.

결제 금액과 상품 구성은 서버 데이터가 기준이다.

PG 응답은 검증 전까지 결제 완료로 확정하지 않는다.

### **5. 도메인 관계와 데이터 소유권**

#### **5.1 핵심 관계**

DOM-01 회원
 ├─ 기본 취향 제공 ───────────────▶ DOM-03 가이드북
 ├─ 관심 장소 회원 식별 ──────────▶ DOM-02 관광 콘텐츠·탐색
 └─ 사용자 식별 ─────────────────▶ DOM-04/05/06

DOM-02 관광 콘텐츠·탐색
 ├─ 장소 원본 제공 ───────────────▶ DOM-03 가이드북
 └─ 장소 원본 제공 ───────────────▶ DOM-04 평가·랭킹

DOM-03 가이드북
 ├─ 생성권 예약/사용 확정/예약 해제 요청 ─────────▶ DOM-06 생성권·결제
 ├─ 여행 종료·방문 장소 제공 ───────▶ DOM-04 평가·랭킹
 └─ 생성/공유 사건 발행 ───────────▶ DOM-05 알림

DOM-04 평가·랭킹
 ├─ 랭킹 결과 제공 ───────────────▶ DOM-02 관광 콘텐츠·탐색
 └─ 평가 요청/완료 사건 발행 ───────▶ DOM-05 알림

#### **5.2 데이터 소유 원칙**

한 데이터의 최종 수정 권한은 하나의 도메인만 가진다.

다른 도메인은 원본을 복제해 수정하지 않고 식별자로 참조한다.

화면 응답은 여러 도메인의 데이터를 조합할 수 있으나, 조합 화면이 데이터 소유권을 갖지는 않는다.

랭킹·검색 결과처럼 재생성 가능한 데이터는 파생 데이터로 구분한다.

결제·생성권·평가 원본은 이력 보존이 중요하므로 물리 삭제보다 상태 전이와 감사 기록을 우선한다.

#### **5.3 공통 불변식**

탈퇴·정지 회원은 인증이 필요한 신규 작업을 수행할 수 없다.

존재하지 않거나 비공개인 관광 콘텐츠는 새 일정에 추가할 수 없다.

동일한 외부 결제 결과는 여러 번 수신돼도 잔액을 한 번만 변경한다.

한 생성 작업에는 하나의 생성권 예약만 연결한다. 예약은 성공 시 한 번만 사용 확정하고, 실패·취소 시 한 번만 해제한다.

동일 회원은 동일 평가 세션의 동일 장소에 하나의 최종 별점만 가진다.

만료되었거나 존재하지 않는 공유 링크로는 가이드북을 가져올 수 없다.

알림 실패가 원본 업무의 성공을 취소하지 않는다.

### **6. 용어 사전**

| **용어** | **정의** | **소유 도메인** |
| --- | --- | --- |
| 회원 | 서비스 계정으로 식별되는 사용자 | DOM-01 |
| 소셜 계정 | OAuth 공급자 계정과 회원의 연결 정보 | DOM-01 |
| 기본 취향 | 가이드북 생성에 기본으로 사용하는 회원의 현재 취향 선택 | DOM-01 |
| 관광 콘텐츠 | 장소·행사·문화유산·명소를 공통 형식으로 표현한 기준 정보 | DOM-02 |
| 행사 | 시작일·종료일 등 기간 속성을 가진 관광 콘텐츠 | DOM-02 |
| 관심 장소 | 회원이 저장한 관광 콘텐츠 참조 | DOM-02 |
| 가이드북 | 여행 조건과 일자별 일정으로 구성된 회원 소유 결과물 | DOM-03 |
| 일정 항목 | 특정 일자와 순서에 배치된 방문 장소 | DOM-03 |
| 생성 작업 | AI 가이드북 생성의 비동기 처리 단위 | DOM-03 |
| 공유 링크 | 가이드북 미리보기·가져오기에 사용하는 만료 가능한 토큰 | DOM-03 |
| 가져오기 | 공유받은 가이드북을 수신 회원의 가이드북으로 추가하는 행위 | DOM-03 |
| 평가 세션 | 한 종료 여행의 방문 장소 평가 진행 상태 | DOM-04 |
| 장소 평가 | 회원이 종료된 가이드북 일정에 포함된 장소에 남긴 별점 | DOM-04 |
| 랭킹 스냅샷 | 특정 일간·주간·월간 기준으로 계산·고정된 순위 집합 | DOM-04 |
| 알림 | 도메인 사건을 사용자에게 보여주는 전달 데이터 | DOM-05 |
| 생성권 | 가이드북 생성 1회에 사용할 수 있는 수량형 권리 | DOM-06 |
| 생성권 원장 | 생성권의 지급·구매·사용·환불·관리자 조정 내역 | DOM-06 |
| 주문 | 사용자가 특정 상품을 구매하려는 거래 단위 | DOM-06 |

**용어 사용 주의**

‘취향’은 별도 여행별 엔티티가 아니라 **회원 기본 취향**을 의미한다.

‘콘텐츠’와 ‘장소’가 혼용되는 화면에서는 API·ERD 기준 용어를 TourismContent로 통일한다.

‘생성권’, ‘이용권’, ‘횟수’가 혼용될 경우 기획 문구를 정리하되 내부 모델은 Entitlement로 통일한다.

### **7. 핵심 사용자 시나리오**

#### **US-01 가입과 초기 설정**

사용자는 소셜 로그인으로 가입하고 약관에 동의한 뒤 기본 취향을 선택한다. 시스템은 회원·세션·동의 이력·기본 취향을 저장하고 메인 화면으로 이동시킨다.

#### **US-02 관광 콘텐츠 탐색**

사용자는 검색어와 지역·월·카테고리 필터를 AND 조건으로 적용한다. 검색 결과를 목록이나 지도에서 확인하고 관심 장소를 저장할 수 있다.

#### **US-03 가이드북 생성**

사용자는 여행 조건을 입력한다. 시스템은 사용 가능한 생성권 1회를 예약한 뒤 회원 기본 취향과 관광 콘텐츠를 사용하여 AI 생성 작업을 수행한다. 성공하면 새로운 가이드북을 저장하고 예약 생성권의 사용을 확정한다. 실패하거나 취소되면 가이드북을 생성하지 않고 예약을 해제한다. 사용자가 기존 결과를 재생성하면 원본 가이드북을 덮어쓰지 않고 원본 ID와 입력 스냅샷을 참조하는 새로운 생성 작업을 만들며, 성공 결과도 별도의 가이드북으로 저장한다.

#### **US-04 일정 확인과 수정**

사용자는 날짜별 추천 장소와 순서를 확인하고 허용된 범위에서 일정을 수정한다. 각 일정 항목은 관광 콘텐츠 원본을 참조한다.

#### **US-05 공유와 가져오기**

사용자는 가이드북 공유 링크를 발급하거나 파일로 내보낸다. 로그인한 수신자는 유효한 링크를 열어 미리 본 뒤 자신의 가이드북으로 가져온다.

#### **US-06 여행 종료 후 평가**

일정 종료 다음 날 이후 첫 앱 접속 시 평가 안내가 표시된다. 모달에서 `다음에 하기`를 선택하면 자동 모달은 다시 표시하지 않지만 가이드북 목록의 평가 버튼으로 재진입할 수 있다. 평가 중에는 방문 장소를 일정 순서대로 확인하며, 0~5점을 입력하거나 해당 장소만 건너뛸 수 있다.

#### **US-07 랭킹 확인**

사용자는 랭킹 보기로 이동해 지역과 일간·주간·월간 조건을 선택한다. 시스템은 평가 수를 보정한 순위와 관광 콘텐츠 정보를 조합해 제공한다.

#### **US-08 생성권 구매**

사용자는 상품을 선택하고 결제한다. 서버가 PG 결과와 금액을 검증한 뒤 생성권 원장을 증가시킨다. 중복 콜백은 같은 거래로 처리한다.

### **8. 비즈니스 규칙**

#### **8.1 DOM-01 회원**

**BR-MEM-01** OAuth 공급자와 공급자 사용자 ID 조합은 중복될 수 없다.

**BR-MEM-02** 로그인 성공 후에만 인증 세션을 발급한다.

**BR-MEM-03** 탈퇴 회원의 활성 세션은 모두 폐기한다.

**BR-MEM-04** 필수 약관의 현재 버전에 동의하지 않은 회원은 핵심 서비스를 사용할 수 없다.

**BR-MEM-05** 약관 동의는 문서 ID, 버전, 동의 시각과 함께 기록한다.

**BR-MEM-06** 회원은 하나의 현재 기본 취향 프로필을 가진다.

**BR-MEM-07** GUIDE-02에서 저장한 취향은 별도 여행 취향이 아니라 회원 기본 취향을 갱신한다.

**BR-MEM-08** 언어 설정이 없으면 서비스 기본 언어를 사용한다.

**BR-MEM-09** 탈퇴 상태에서는 신규 가이드북 생성, 평가, 결제를 허용하지 않는다.

#### **8.2 DOM-02 관광 콘텐츠·탐색**

**BR-CON-01** 관광 콘텐츠의 외부 출처 ID와 출처 조합은 유일해야 한다.

**BR-CON-02** 검색어와 복수 필터는 AND 조건으로 적용한다.

**BR-CON-03** 지역 ‘전체’, 월 ‘전체’는 해당 조건을 제한하지 않는 것과 같다.

**BR-CON-04** 월 필터는 기간을 가진 행사에 적용한다. 상시 장소에 대한 적용 방식은 별도 확정한다.

**BR-CON-05** 검색어는 최대 10자이며 엔터 입력 시 서버 검색을 수행한다.

**BR-CON-06** 결과가 없으면 검색·필터 공통 빈 상태를 제공한다.

**BR-CON-07** 관심 장소 등록은 회원과 관광 콘텐츠 조합당 하나만 존재한다.

**BR-CON-08** 현재 위치 권한이 허용되지 않은 채로 앱을 사용하려 하면 허용 요청 알림이 지속된다.

**BR-CON-09** 검색·필터·랭킹 조회는 관광 콘텐츠 원본을 변경하지 않는다.

#### **8.3 DOM-03 가이드북**

**BR-GDE-01** 시작일은 종료일보다 늦을 수 없으며 최대 여행 기간인 7일을 초과할 수 없다.

**BR-GDE-02** 유효한 여행 조건과 회원 기본 취향이 존재하고 사용 가능한 생성권 1회를 예약할 수 있어야 생성 작업을 시작한다.

**BR-GDE-03** 하나의 생성 요청 키는 하나의 생성 작업과 하나의 생성권 예약에만 대응한다.

**BR-GDE-04**  AI 생성이 성공하면 새로운 가이드북을 저장한다. 생성에 실패하면 가이드북을 생성하지 않는다.

**BR-GDE-05** 생성이 최종 실패하거나 취소되면 동일 생성 작업의 생성권 예약을 한 번만 해제한다.

**BR-GDE-06** 일정 항목의 날짜는 가이드북 여행 기간 안에 있어야 한다.

**BR-GDE-07** 같은 일자의 순서는 중복되지 않으며 변경 시 연속된 순서로 재정렬한다.

**BR-GDE-08** 공유 링크는 원문 ID를 노출하지 않는 난수 토큰을 사용하며 만료 시 사용할 수 없다.

**BR-GDE-09** 가져온 가이드북은 수신 회원이 소유하는 독립 복사본으로 저장한다. 원본 이후 변경은 복사본에 전파하지 않는다.

**BR-GDE-10** 동일 회원은 하나의 공유 링크를 통해 가이드북을 한 번만 가져올 수 있다. 이미 가져온 링크에서 다시 추가를 요청하면 새로운 복사본을 생성하지 않고, 기존에 추가된 가이드북으로 안내한다.

**BR-GDE-11** PDF 생성 실패가 가이드북 원본 상태를 변경하지 않는다.

**BR-GDE-12** HTML/PDF에 포함되는 개인정보 범위는 최소화한다.

**BR-GDE-13** 사용자의 재생성 요청은 기존 가이드북을 덮어쓰지 않고 새로운 생성 작업으로 처리한다. 재생성 작업은 `source_guidebook_id`로 원본 가이드북을 참조하고 요청 시점의 입력 스냅샷을 보존한다. 성공하면 별도의 새 가이드북을 생성하고, 실패하면 원본 가이드북을 변경하지 않는다.

#### **8.4 DOM-04 평가·랭킹**

**BR-RNK-01** 일정 종료 다음 날 이후의 가이드북만 평가할 수 있다.

**BR-RNK-02** 평가 대상은 해당 가이드북 일정에 포함된 장소다. 시스템은 실제 방문 여부를 별도로 검증하지 않는다.

**BR-RNK-03** 별점은 0점부터 5점까지 허용한다. 0점도 유효한 평가로 저장하고 평균과 랭킹 집계에 포함한다. 장소를 건너뛴 경우 점수는 `NULL`로 처리하고 유효 평가 수와 평균 계산에서 제외한다.

**BR-RNK-04** 이전·다음 이동 중 입력값을 보존하고 제출 전에는 수정할 수 있다. 별점을 선택한 상태에서 다음 장소로 이동하면 해당 점수를 반영한다.

**BR-RNK-05** 평가 안내 모달에서 `다음에 하기`를 선택하면 이후 앱 접속 시 자동 모달을 다시 표시하지 않는다. 평가를 포기한 것은 아니며 가이드북 목록의 평가 버튼을 통해 다시 시작할 수 있다.

**BR-RNK-06** 장소 평가에서 `이 장소 건너뛰기`를 선택하면 해당 장소의 점수를 `NULL`로 두고 다음 장소로 이동한다. 제출 전 이전 장소로 돌아가 점수를 입력하거나 변경할 수 있다.

**BR-RNK-07** 같은 회원·가이드북·장소의 최종 평가는 하나만 존재한다.

**BR-RNK-08** 제출 요청이 중복돼도 평가와 집계 반영은 한 번만 수행한다.

**BR-RNK-09** 랭킹은 베이지안 가중 평점을 기준으로 산정한다.

**BR-RNK-10** 랭킹 기간은 일간·주간·월간을 지원한다.

**BR-RNK-11** 유효 평가가 없는 집계 구간은 ‘랭킹 결과 없음’으로 제공한다.

**BR-RNK-12** 동점은 가중 평점, 유효 평가 수, 원평균, 콘텐츠 ID 등 확정된 보조 기준으로 안정 정렬한다.

**BR-RNK-13** 삭제·무효 처리된 평가와 점수가 `NULL`인 장소는 다음 집계에서 제외한다.

#### **8.5 DOM-05 알림**

**BR-NOT-01** 알림 생성 실패는 원본 업무를 롤백하지 않는다.

**BR-NOT-02** 같은 사건과 수신자 조합의 알림은 중복 생성하지 않는다.

**BR-NOT-03** 회원은 자신의 알림만 조회·읽음 처리할 수 있다.

**BR-NOT-04** 모두 읽음은 요청 시점 이전의 미읽음 알림을 대상으로 한다.

**BR-NOT-05** 푸시 수신 거부 시에도 필요한 인앱 알림은 저장할 수 있다.

**BR-NOT-06** 목록 최대 노출 수는 V3 기준 20개로 해석하되 초기 노출 개수는 4개이다.

#### **8.6 DOM-06 생성권·결제**

**BR-PAY-01** 생성권 원장 잔액은 회원 테이블의 숫자를 직접 수정하지 않고 원장 내역으로 산정한다. 생성에 사용할 수 있는 수량은 원장 잔액에서 활성 예약 수량을 제외하여 판단한다.

**BR-PAY-02** 원장 항목은 지급·구매·사용·환불·관리자 조정 사유를 가진다. 생성 중 임시 확보는 생성권 예약으로 별도 관리한다.

**BR-PAY-03** 사용 가능한 생성권이 부족하면 예약을 생성하지 않고 AI 생성 작업도 시작하지 않는다.

**BR-PAY-04** 생성권 예약은 조건부 원자 연산으로 처리하여 원장 잔액을 초과한 예약이 생성되지 않도록 한다. 성공 시 예약의 사용 확정과 가이드북 저장은 중복 없이 처리한다.

**BR-PAY-05** 주문 금액은 클라이언트 값이 아니라 서버의 상품 정보로 검증한다.

**BR-PAY-06** 동일 PG 거래 ID 또는 멱등 키는 한 번만 결제 완료 처리한다.

**BR-PAY-07** 검증되지 않은 웹훅은 주문과 잔액을 변경하지 않는다.

**BR-PAY-08** 환불 성공 확인 후에만 생성권 회수 또는 환불 원장을 반영한다.

**BR-PAY-09** 이미 사용된 유료 생성권의 환불 가능 범위는 정책 확정이 필요하다.

**BR-PAY-10** 결제·환불·원장 내역은 감사와 분쟁 대응을 위해 보존한다.

### **9. 기능 요구사항**

| **ID** | **도메인** | **기능 요구사항** | **우선순위** | 버전별 구현 |
| --- | --- | --- | --- | --- |
| FR-MEM-01 | DOM-01 | 소셜 OAuth 로그인과 회원 생성을 제공한다. | P0 | v1 |
| FR-MEM-02 | DOM-01 | 세션 갱신, 로그아웃, 탈퇴를 제공한다. | P0 | v1 |
| FR-MEM-03 | DOM-01 | 최초·마이페이지 기본 취향 조회 및 수정을 제공한다. | P0 | v1 |
| FR-MEM-04 | DOM-01 | 프로필과 표시 언어를 조회·수정한다. | P1 | v2 |
| FR-MEM-05 | DOM-01 | 약관 버전과 회원 동의 이력을 저장한다. | P0 | v3 |
| FR-CON-01 | DOM-02 | 관광 콘텐츠 목록과 상세를 제공한다. | P0 | v1 |
| FR-CON-02 | DOM-02 | 검색어 및 지역·월·카테고리 필터를 제공한다. | P0 | v2 |
| FR-CON-03 | DOM-02 | 페이지네이션과 결과 없음 상태를 제공한다. | P0 | v1 |
| FR-CON-04 | DOM-02 | 위치·지도 범위 기반 마커와 클러스터를 제공한다. | P0 | v1 |
| FR-CON-05 | DOM-02 | 관심 장소 등록·해제·목록 조회를 제공한다. | P1 | v1 |
| FR-GDE-01 | DOM-03 | 여행 조건을 검증하고 가이드북 생성 요청을 접수한다. | P0 | v1 |
| FR-GDE-02 | DOM-03 | AI 생성 작업 상태를 조회하고, 성공 시 새로운 가이드북 저장과 생성권 사용 확정을 수행한다. | P0 | v1 |
| FR-GDE-03 | DOM-03 | 생성순 조회를 제공한다. | P0 | v1 |
| FR-GDE-04 | DOM-03 | 공유 링크 발급·검증·미리보기를 제공한다. | P0 | v3 |
| FR-GDE-05 | DOM-03 | 공유받은 가이드북 가져오기를 제공한다. | P0 | v3 |
| FR-GDE-06 | DOM-03 | HTML 보기와 PDF 내보내기를 제공한다. | P1 | v1 |
| FR-GDE-07 | DOM-03 | 기존 가이드북을 참조하는 재생성 작업을 요청하고 성공 결과를 별도의 가이드북으로 저장한다. | P0 | v3 |
| FR-RNK-01 | DOM-04 | 여행 종료 후 평가 대상과 평가 진행 상태를 제공한다. | P0 | v3 |
| FR-RNK-02 | DOM-04 | 장소별 별점 입력·수정·제출·건너뛰기를 제공한다. | P0 | v3 |
| FR-RNK-03 | DOM-04 | 일간·주간·월간 랭킹을 계산하고 조회한다. | P0 | v3 |
| FR-RNK-04 | DOM-04 | 랭킹 결과 없음 상태를 제공한다. | P0 | v3 |
| FR-NOT-01 | DOM-05 | 인앱 알림 목록과 미읽음 상태를 제공한다. | P0 | v1 |
| FR-NOT-02 | DOM-05 | 개별 읽음과 모두 읽음을 제공한다. | P0 | v1 |
| FR-NOT-03 | DOM-05 | 푸시 수신 설정을 조회·변경한다. | P1 | v1 |
| FR-PAY-01 | DOM-06 | 생성권 잔액과 원장 내역을 조회한다. | P0 | v2 |
| FR-PAY-02 | DOM-06 | 가이드북 생성 전 생성권을 멱등하게 예약하고, 생성 성공 시 사용을 확정하며, 실패·취소 시 예약을 해제한다. | P0 | v2 |
| FR-PAY-03 | DOM-06 | 상품 조회, 주문 생성, 결제 검증을 제공한다. | P0 | v2 |
| FR-PAY-04 | DOM-06 | 환불 요청과 결과 반영을 제공한다. | P1 | v2 |

### **10. 비기능 요구사항**

#### **10.1 성능**

**NFR-PERF-01** 검색·필터·랭킹 목록 API는 정상 부하에서 p95 1초 이내를 목표로 한다.

**NFR-PERF-02** 지도 이동 중 조회는 디바운스와 영역 기반 요청을 사용하고 한 응답의 마커 수를 제한한다.

**NFR-PERF-03** AI 생성, PDF 생성, 랭킹 집계는 동기 HTTP 요청에서 장시간 점유하지 않고 작업 상태를 조회할 수 있게 한다.

**NFR-PERF-04** 랭킹은 요청마다 전체 평가를 계산하지 않고 스냅샷 또는 캐시를 사용한다.

#### **10.2 보안과 개인정보**

**NFR-SEC-01** OAuth 토큰, PG 비밀키, 서명키는 클라이언트와 로그에 노출하지 않는다.

**NFR-SEC-02** 모든 회원 데이터 조회·변경 API는 리소스 소유권을 검증한다.

**NFR-SEC-03** 공유 토큰은 추측이 어려운 난수이며 접근 시 `expires_at`을 검증한다.

**NFR-SEC-04** 결제 웹훅은 서명·거래 ID·금액·주문 상태를 검증한다.

**NFR-SEC-05** 로그와 내보내기 파일에는 불필요한 개인정보를 포함하지 않는다.

**NFR-SEC-06** 탈퇴 시 개인정보 파기와 법정 보존 데이터 분리를 지원한다.

#### **10.3 데이터 정합성**

**NFR-DAT-01** 회원, 평가, 가이드북, 원장 변경은 DB 제약과 트랜잭션으로 불변식을 보장한다.

**NFR-DAT-02** 결제 웹훅, 평가 제출, 생성 요청, 가져오기는 멱등 키 또는 유일 제약을 가진다.

**NFR-DAT-03** 외부 시스템과의 분산 작업은 재시도 가능한 상태와 보상 처리를 둔다.

**NFR-DAT-04** 파생 데이터는 원본과 집계 기준 시각을 기록해 재생성 가능해야 한다.

#### **10.4 가용성과 복구**

**NFR-AVL-01** 외부 관광 API 장애 시 마지막 정상 데이터 또는 명확한 오류 상태를 제공한다.

**NFR-AVL-02** AI·PG·푸시 실패를 구분해 재시도 가능 여부를 기록한다.

**NFR-AVL-03** 작업 재시도 후에도 중복 가이드북, 중복 생성권 예약·사용, 중복 알림이 생성되지 않아야 한다.

**NFR-AVL-04** 중요 데이터는 백업과 복원 절차를 갖고 복원 검증을 수행한다.

#### **10.5 확장성과 운영**

**NFR-OPS-01** 도메인별 패키지와 의존 방향을 구분해 추후 모듈 분리가 가능하게 한다.

**NFR-OPS-02** 생성 성공률, 생성 시간,결제·웹훅 실패, 생성권 예약 해제 실패, 랭킹 집계 실패, 푸시 실패를 모니터링한다.

**NFR-OPS-03** 모든 비동기 작업은 작업 ID, 회원 ID, 원인 ID를 이용해 추적 가능해야 한다.

**NFR-OPS-04** 운영자가 실패 작업과 원장 변경 이력을 조회할 수 있어야 한다.

### **11. 주요 상태 모델**

#### **11.1 회원**

ONBOARDING → ACTIVE → WITHDRAWN

상태 전이와 별개로 세션은 즉시 폐기할 수 있어야 한다.

#### **11.2 가이드북 생성 작업**

```
GenerationJob
QUEUED → PROCESSING → SUCCEEDED
                     └→ FAILED
```

생성 작업이 `SUCCEEDED`로 전환될 때 새로운 가이드북을 생성한다. `FAILED`인 경우 가이드북을 생성하지 않는다.

시스템 오류에 대한 재시도는 동일한 생성 작업에서 최대 3회 수행하고 `attempt_count`로 관리한다. 사용자가 결과를 확인한 뒤 요청하는 재생성은 새로운 생성 작업이며, `source_guidebook_id`와 입력 스냅샷으로 원본을 참조한다.

#### **11.3 생성권 예약**

```
RESERVED → CONSUMED
         └→ RELEASED
```

하나의 생성 작업에는 하나의 생성권 예약만 연결한다. 생성 성공 시 `CONSUMED`, 생성 실패·취소 시 `RELEASED`로 한 번만 전환한다. 동일한 결과가 반복 전달돼도 사용 확정이나 예약 해제를 중복 처리하지 않는다.

#### **11.4 공유 링크**

공유 링크는 별도의 `ACTIVE`·`EXPIRED` 상태값을 저장하지 않는다. 생성 시 `expires_at`을 저장하고 접근 시 현재 시각과 비교해 유효성을 판단한다. 공유 화면의 취소는 링크 상태를 변경하지 않으며, 만료되었거나 존재하지 않는 링크는 동일한 오류로 안내한다.

#### **11.5 평가 요청 및 평가 세션**

```
PENDING → IN_PROGRESS → SUBMITTED
       └→ DEFERRED → IN_PROGRESS → SUBMITTED
```

`DEFERRED`는 평가 안내 모달에서 `다음에 하기`를 선택해 자동 모달 재노출만 중단한 상태다. 사용자는 가이드북 목록의 평가 버튼으로 `IN_PROGRESS`에 진입할 수 있다.

장소별 입력 상태는 제출 전 다음과 같이 관리한다.

```
UNANSWERED → RATED(score: 0~5)
           └→ SKIPPED(score: NULL)
```

0점은 유효한 평가이며 랭킹 집계에 포함한다. `NULL`은 해당 장소를 평가하지 않은 것으로 보고 집계에서 제외한다. 제출 전에는 이전 장소로 돌아가 `RATED`와 `SKIPPED`를 변경할 수 있다.

#### **11.6 주문·결제**

주문: CREATED → PAYMENT_PENDING → PAID | FAILED | CANCELED

주문과 결제 시도는 1:N으로 두어 재시도를 표현한다.

### **12. 정상·예외·경계·복구 시나리오**

| **ID** | **유형** | **상황** | **기대 처리** |
| --- | --- | --- | --- |
| SCN-01 | 정상 | 신규 회원이 로그인·동의·취향 선택 완료 | 회원과 기본 취향 저장 후 활성화 |
| SCN-02 | 예외 | OAuth 이메일은 같지만 공급자 식별자가 다름 | 자동 병합하지 않고 계정 연결 정책에 따라 처리 |
| SCN-03 | 경계 | 검색어가 빈 값이거나 10자를 초과 | 빈 값은 전체 조회, 초과는 입력 차단 |
| SCN-04 | 정상 | 검색어와 세 필터를 함께 적용 | 모든 조건을 AND로 반영 |
| SCN-05 | 예외 | 위치 권한 거부 | 권한 획득 하지 못 할 시 어플리케이션 이용 제한 |
| SCN-06 | 예외 | 외부 관광 API 장애 | 저장된 데이터 제공 또는 재시도 가능한 오류 표시 |
| SCN-07 | 정상 | 생성권 1개로 가이드북 생성 성공 | 예약 생성권을 사용 확정하고 새로운 가이드북을 저장한다. 생성 작업을 `SUCCEEDED`로 변경하고 완료 알림을 생성한다. |
| SCN-08 | 복구 | 생성권 예약 후 AI 생성 최종 실패 | 가이드북을 생성하지 않고 생성 작업을 `FAILED`로 변경한 뒤 예약을 한 번만 해제한다. |
| SCN-09 | 중복 | 사용자가 생성 버튼을 연속 탭 | 같은 멱등 키로 생성 작업과 생성권 예약을 각각 한 건만 생성한다. |
| SCN-10 | 경계 | 시작일과 종료일이 같음 | 당일 여행을 허용한다면 1일 일정으로 생성 |
| SCN-11 | 예외 | 만료된 공유 링크 접근 | 만료 안내, 미리보기·가져오기 차단 |
| SCN-12 | 중복 | 같은 링크 가져오기 반복 | 확정된 중복 정책에 따라 기존 복사본 안내 또는 새 복사본 생성 |
| SCN-13 | 예외 | PDF 생성 실패 | 가이드북은 유지하고 내보내기만 재시도 |
| SCN-14 | 정상 | 여행 종료 후 장소 평가 제출 | 0~5점이 입력된 장소만 유효 평가로 저장하고 `NULL`인 장소는 집계에서 제외한다. 세션은 `SUBMITTED`로 변경한다. |
| SCN-15 | 정상 | 평가 안내에서 `다음에 하기` 선택 후 가이드북에서 재진입 | 자동 모달은 재표시하지 않고 수동 평가를 허용한다. |
| SCN-16 | 중복 | 제출 응답 지연으로 평가 재요청 | 유일 제약·멱등 키로 한 번만 반영 |
| SCN-17 | 경계 | 집계 기간에 유효 평가가 없음 | 빈 랭킹 화면 제공 |
| SCN-18 | 경계 | 두 장소의 가중 평점이 같음 | 확정된 보조 기준으로 안정 정렬 |
| SCN-19 | 예외 | 푸시 발송 실패 | 원본 업무 유지, 재시도 후 실패 기록 |
| SCN-20 | 중복 | PG 웹훅이 여러 번 도착 | 결제 완료와 생성권 지급을 한 번만 수행 |
| SCN-21 | 공격 | 클라이언트가 상품 금액을 변조 | 서버 상품 금액 불일치로 결제 확정 거부 |
| SCN-22 | 복구 | 결제 완료 후 생성권 지급 실패 | 재처리 작업으로 원장 반영, 운영 경보 발생 |
| SCN-23 | 정상 | 기존 가이드북 재생성 성공 | 원본 가이드북을 유지하고 `source_guidebook_id`를 참조하는 새 가이드북을 생성한다. |
| SCN-24 | 경계 | 0점 평가와 장소 건너뛰기가 함께 존재 | 0점은 유효 평가로 집계하고 `NULL`인 건너뛴 장소는 집계에서 제외한다. |

### **13. ERD 설계 초안**

#### **13.1 도메인별 테이블 후보**

| **도메인** | **테이블 후보** |
| --- | --- |
| DOM-01 | members, social_accounts, sessions, preference_profiles, preference_options, preference_selections, member_settings, policy_documents, member_consents |
| DOM-02 | tourism_contents, event_details, content_categories, content_category_mappings, content_images, content_sources, favorite_contents |
| DOM-03 | guidebooks, trip_conditions, generation_jobs, itinerary_days, itinerary_items, share_links, guidebook_imports, guidebook_exports |
| DOM-04 | rating_sessions, place_ratings, ranking_snapshots, ranking_entries |
| DOM-05 | notifications, push_preferences |
| DOM-06 | products, orders, payment_attempts, refunds, entitlement_reservations, entitlement_ledger, payment_policies |

#### **13.2 주요 관계**

Member 1:N SocialAccount

Member 1:1 PreferenceProfile

PreferenceProfile N:M PreferenceOption

Member N:M TourismContent — FavoriteContent로 연결

Member 1:N GenerationJob

Member 1:N Guidebook

GenerationJob N:1 SourceGuidebook — 재생성인 경우 `source_guidebook_id`로 Guidebook 참조

GenerationJob 1:1 TripCondition

GenerationJob 1:0..1 Guidebook — 생성 성공 시에만 결과 가이드북 존재

Guidebook 1:N ItineraryDay

ItineraryDay 1:N ItineraryItem

ItineraryItem N:1 TourismContent

Guidebook 1:N ShareLink

ShareLink 1:N GuidebookImport

Guidebook 1:N RatingSession 또는 회원·가이드북 조합 1:1

RatingSession 1:N PlaceRating

PlaceRating N:1 TourismContent

RankingSnapshot 1:N RankingEntry

RankingEntry N:1 TourismContent

Member 1:N Notification

Member 1:N Order

Order 1:N PaymentAttempt

Member 1:N EntitlementReservation

GenerationJob 1:1 EntitlementReservation

Member 1:N EntitlementLedger

#### **13.3 필수 제약 후보**

social_accounts(provider, provider_user_id) UNIQUE

preference_profiles(member_id) UNIQUE

preference_selections(profile_id, option_id) UNIQUE

favorite_contents(member_id, content_id) UNIQUE

generation_jobs(idempotency_key) UNIQUE

guidebooks(generation_job_id) UNIQUE

generation_jobs의 `INITIAL`은 source_guidebook_id가 NULL, `REGENERATION`은 NOT NULL

itinerary_items(day_id, sequence) UNIQUE

share_links(token_hash) UNIQUE

place_ratings(member_id, guidebook_id, content_id) UNIQUE

place_ratings.score CHECK (score IS NULL OR score BETWEEN 0 AND 5)

ranking_snapshots(period_type, period_start, region_code) UNIQUE 후보

notifications(recipient_id, source_event_id, type) UNIQUE

payment_attempts(pg_provider, pg_transaction_id) UNIQUE

entitlement_reservations(generation_job_id) UNIQUE

entitlement_ledger(reason_type, reason_id, member_id) UNIQUE 후보

#### **13.4 ERD에 만들지 않을 항목**

검색 조건, 드롭다운 열림 상태, 현재 지도 범위는 요청 또는 UI 상태다.

Marker와 Cluster는 지도 응답 모델이며 원본 테이블이 아니다.

베이지안 계산식은 도메인 규칙이며 별도 엔티티가 아니다. 단, 집계 당시의 C, m, 계산 버전은 스냅샷 메타데이터로 저장할 수 있다.

V3에서 근거가 없는 여행별 PreferenceSnapshot 엔티티는 만들지 않는다.

### **14. API·비동기 작업 설계 방향**

#### **14.1 API 경계 예시**

DOM-01: /auth, /members/me, /members/me/preferences, /policies, /consents

DOM-02: /contents, /contents/{id}, /map/contents, /favorites

DOM-03: /guidebooks, /guidebooks/{id}/itinerary, /guidebooks/{id}/shares, /imports, /exports

DOM-04: /rating-sessions, /ratings, /rankings

DOM-05: /notifications, /notifications/read-all, /push-preferences

DOM-06: /entitlements, /products, /orders, /payments/webhooks, /refunds

#### **14.2 비동기 작업**

관광 데이터 동기화

AI 가이드북 생성

PDF 생성

평가 집계와 일간·주간·월간 랭킹 스냅샷 생성

푸시 발송과 재시도

결제 후 생성권 지급 재처리

#### **14.3 도메인 간 정합성 방식**

한 DB 트랜잭션으로 처리 가능한 도메인 내부 변경은 즉시 일관성을 사용한다.

AI·PG·푸시처럼 외부 시스템이 개입하는 작업은 상태 머신, 멱등 키, 재시도, 보상 작업을 사용한다.

가이드북 생성과 생성권 예약·사용 확정은 초기에는 동일 애플리케이션 서비스에서 오케스트레이션하되 각 도메인의 변경 API를 분리한다.

도메인 사건 발행이 DB 커밋과 분리돼 유실될 위험이 있으면 Outbox 패턴을 적용한다.

### **15. 프론트엔드와 논의할 사항**

#### **공통**

API 오류 코드별 화면 문구, 재시도 버튼, 로그인 이동 기준

서버 상태와 로컬 UI 상태의 구분

중복 탭 방지를 위한 버튼 비활성화 및 로딩 표현

#### **관광 콘텐츠·탐색**

검색어 10자 초과 시 입력 자체 차단과 서버 오류 중 선택

월 필터를 행사에만 적용할지, 상시 장소에는 숨기거나 무시할지

‘전체’ 선택 시 버튼 라벨과 초기화 기준

랭킹 화면 진입 시 기존 검색어·월·카테고리 상태를 보존할지 초기화할지

랭킹 화면에서 지역·일간/주간/월간 조건만 제공하는지 최종 확인

지도 권한 거부·위치 조회 실패·결과 없음 상태

#### **가이드북**

생성 작업 대기 중 앱 이탈·재접속 시 진행 상태 복원 방식

생성 실패와 생성권 예약 해제 완료를 어떤 순서와 문구로 보여줄지

공유 링크 만료 화면, 비로그인 사용자의 로그인 후 원래 링크 복귀

중복 가져오기 시 안내 문구

HTML/PDF 내보내기 진행·실패 표시

#### **평가·랭킹**

별점 범위와 최소 입력 단위

일부 장소만 입력한 상태에서 제출 가능 여부

평가 안내에서 `다음에 하기`를 선택한 경우 가이드북 평가 버튼 노출 조건

장소별 `이 장소 건너뛰기` 선택 시 `NULL` 표시와 이전 장소 재진입 방식

랭킹 동점 표시와 결과 없음 화면

일간·주간·월간 기준 시각과 화면 표기

#### **알림**

초기 노출 4개/5개 불일치 해결

최대 20개 이후 페이지네이션 또는 더보기 방식

푸시 비활성화 시 OS 설정 이동 여부

#### **생성권·결제**

결제 진행·취소·실패·완료 후 복귀 화면

결제 완료 후 생성권 반영 지연 시 안내와 새로고침

환불 가능 조건과 사용된 생성권 표시

### **16. 기획·화면 정의에서 확정이 필요한 항목**

| **ID** | **항목** | **현재 해석** | **결정 필요 내용** |
| --- | --- | --- | --- |
| DEC-01 | 기본 취향 | 회원에게 하나의 현재 기본 취향이 있음 | 기존 가이드북 카드가 생성 당시 취향을 보존 표시할지 현재 기본값을 표시할지 |
| DEC-02 | 월 필터 | 기간형 행사에 자연스럽게 적용 | 명소·문화유산 포함 목록에서 월 필터 동작 |
| DEC-03 | 랭킹 기간 | 일간·주간·월간 제공 | 일·주·월 시작 기준, 타임존, 갱신 시각 |
| DEC-04 | 랭킹 공식 | 베이지안 가중 평균 사용 | 최소 평가 수 m, 모집단 평균 C 범위, 동점 규칙 |
| DEC-05 | 평가 | 0~5점은 유효 평가, 장소 건너뛰기는 `NULL` | 부분 제출과 제출 후 수정 가능 여부 |
| DEC-06 | 공유 링크 | `expires_at`으로 만료 여부 계산 | 기본 만료 시간과 로그인 전 미리보기 범위 |
| DEC-07 | 가져오기 | 독립 복사본으로 저장 | 같은 링크 반복 가져오기와 원본 삭제 후 처리 |
| DEC-08 | 알림 | 최대 20개 | 초기 4개/5개, 보관 기간, 삭제 제공 여부 |
| DEC-09 | 생성 실패 | 예약 후 실패·취소 시 예약 해제 | 타임아웃 판정 기준과 동일 작업의 최대 재시도 횟수 |
| DEC-10 | 환불 | PG 성공 확인 후 원장 반영 | 사용된 생성권, 부분 환불, 환불 기한 |
| DEC-11 | 탈퇴 | 세션 즉시 폐기 | 유예 기간과 가이드북·평가·결제 데이터 보존 |

### **17. 요구사항 추적성 예시**

| **사용자 시나리오** | **비즈니스 규칙** | **기능 요구사항** | **예상 설계 산출물** |
| --- | --- | --- | --- |
| US-01 가입과 초기 설정 | BR-MEM-01~09 | FR-MEM-01~05 | 회원·소셜계정·동의·취향 ERD, 인증 API |
| US-02 관광 콘텐츠 탐색 | BR-CON-01~09 | FR-CON-01~05 | 콘텐츠 ERD, 검색/지도 API, 인덱스 |
| US-03 가이드북 생성 | BR-GDE-01~05, BR-PAY-03~04 | FR-GDE-01~02, FR-PAY-01~02 | 생성 상태 머신, 예약/사용 확정/예약 해제 시퀀스 |
| US-05 공유와 가져오기 | BR-GDE-08~12 | FR-GDE-04~06 | 공유 토큰 ERD, 가져오기·내보내기 API |
| US-06 여행 종료 후 평가 | BR-RNK-01~08 | FR-RNK-01~02 | 평가 세션 ERD, 평가 API |
| US-07 랭킹 확인 | BR-RNK-09~13 | FR-RNK-03~04 | 집계 배치, 랭킹 스냅샷 ERD/API |
| US-08 생성권 구매 | BR-PAY-01~10 | FR-PAY-01~04 | 주문·결제·원장 ERD, 웹훅/API |

### **18. 구현 우선순위**

#### **P0: ERD/API 전에 결정**

회원 기본 취향과 기존 가이드북 표시값의 관계

관광 콘텐츠 공통 모델과 행사 전용 속성 분리

월 필터 적용 대상

가이드북 생성권 예약 시점과 사용 확정·예약 해제 기준

별점 범위·단위와 부분 제출 정책

랭킹의 m, C, 집계 기간, 동점 기준

공유 링크 만료와 중복 가져오기 정책

생성권 환불·회수 정책

#### **P1: 구현 중 확정 가능**

알림 초기 노출 수와 보관 기간

PDF 재시도 횟수와 파일 보존 시간

지도 기본 위치와 마커 최대 개수

검색 자동완성·최근 검색 제공 여부

#### **P2: 운영 데이터 이후 개선**

랭킹 m 값 튜닝

검색 인덱스와 랭킹 캐시 분리

추천·취향 고도화 시 DOM-01에서 취향 모듈 분리

트래픽 증가 시 탐색 읽기 모델 또는 검색 서비스 분리

### **19. PL 미팅 설명용 결정 요약**

#### **“왜 6개인가?”**

화면 수가 아니라 데이터 소유권과 상태 전이를 기준으로 나눴다. 회원에게 붙는 기본 취향과 언어·약관 동의는 회원에 합쳤고, 관광 콘텐츠를 조회하는 검색·지도는 콘텐츠에 합쳤다. 공유·가져오기는 가이드북의 전달 생명주기라 가이드북에 포함했다. 반면 평가·랭킹, 알림, 생성권·결제는 각각 독립 상태와 무결성·실패 복구 규칙이 있어 유지했다.

#### **“왜 더 합치지 않았는가?”**

평가를 콘텐츠에 합치면 콘텐츠 원본과 사용자 생성 원본·집계 규칙이 섞인다.

알림을 회원에 합치면 회원이 모든 도메인의 사건과 푸시 실패를 알아야 한다.

생성권·결제를 회원이나 가이드북에 합치면 금전·원장 정합성과 중복 PG 처리가 약해진다.

#### **“왜 더 나누지 않았는가?”**

현재 규모에서는 검색, 지도, 공유, 취향이 독립 배포나 별도 저장소를 요구할 정도의 생명주기를 갖지 않는다. 먼저 모듈러 모놀리스의 패키지 경계를 지키고, 트래픽·팀 규모·변경 빈도가 커질 때 분리하는 편이 구현 가능성과 유지보수에 유리하다.

#### **“이 결정의 단점은?”**

DOM-02와 DOM-03의 책임 범위가 넓어질 수 있다. 이를 완화하기 위해 도메인 내부에서 수집/검색/지도, 생성/일정/공유 같은 하위 패키지를 구분한다. 향후 검색 인프라나 공유 정책이 독립적으로 커지면 현재 경계를 기준으로 하위 모듈을 분리한다.

### **20. 설계 셀프 리뷰**

#### **요구사항 연결**

각 도메인 책임을 사용자 시나리오, 비즈니스 규칙, 기능 요구사항과 ID로 연결했는가?

화면 요소가 아니라 실제 업무 규칙을 설명하고 있는가?

#### **설계 근거**

9개에서 6개로 병합한 기준과 유지한 이유를 설명할 수 있는가?

원본 데이터와 조회·파생 데이터를 구분했는가?

#### **대안과 트레이드오프**

취향, 탐색, 공유, 정책을 독립 도메인으로 두는 대안을 검토했는가?

6개 경계로 인해 커지는 모듈과 향후 분리 조건을 기록했는가?

#### **정상·예외 시나리오**

중복 생성·평가 제출·결제 웹훅을 멱등하게 처리하는가?

외부 API, AI, PDF, PG, 푸시 실패 후 복구 경로가 있는가?

결과 없음, 만료, 권한 거부, 동점, 잔액 부족 등 경계값을 다뤘는가?

#### **문서 간 일관성**

V3 화면설계서, 기능정의서, 본 문서, ERD, API에서 동일 용어와 상태를 쓰는가?

여행별 취향처럼 V3 근거가 없는 개념이 남아 있지 않은가?

#### **구현 가능성**

초기 구현이 6개 마이크로서비스가 아니라 하나의 모듈러 모놀리스임을 팀이 동일하게 이해하는가?

P0 결정 항목을 ERD와 API 작성 전에 확정할 수 있는가?

팀 일정상 P1·P2 항목을 뒤로 미뤄도 핵심 시나리오가 완성되는가?

### **21. 현재 결론**

V3 도메인은 **회원, 관광 콘텐츠·탐색, 가이드북, 평가·랭킹, 알림, 생성권·결제**의 6개로 정의한다.

취향은 회원의 기본 설정, 탐색·지도는 관광 콘텐츠의 조회 기능, 공유·가져오기는 가이드북의 하위 책임으로 병합한다.

서비스 설정·정책은 독립 도메인으로 두지 않고 언어·일반 약관은 회원, 푸시 설정은 알림, 결제·환불 정책은 생성권·결제가 소유한다.

랭킹은 여행 종료 후 제출된 장소 평가를 기반으로 베이지안 가중 평점을 계산하며 **일간·주간·월간을 제공**한다.

다음 단계에서는 P0 결정 항목을 확정한 뒤 이 6개 경계를 기준으로 ERD와 API를 작성한다.
