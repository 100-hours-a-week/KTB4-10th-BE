# 테크스펙 문서 작성

# 도메인 - 회원, 인증, 인가 (V1)

# 배경

- **프로젝트 목표:** 카카오 로그인 후 기본 취향을 저장하고 서비스를 이용할 수 있게 한다.
- **핵심 결과:** 회원 식별, 로그인 유지, 취향 변경, 로그아웃·탈퇴, 알림 조회·삭제를 처리한다.
- **문제 정의:** 로그인 여부만 확인해서는 타인 데이터 접근을 막을 수 없다. 회원 상태와 데이터 접근 권한을 함께 확인해야 한다.
- **가설:** 서버 세션으로 회원을 확인하고, 함께 바뀌어야 하는 데이터는 하나의 트랜잭션으로 처리하면 회원 상태를 일관되게 관리할 수 있다.
- **관련 자료:**
  - [요구사항정의서](../요구사항정의서.md)
  - [V3 API 명세서](../V3%20API%20명세서.md)
  - [테이블정의서](../테이블정의서.md)
  - [회원 도메인 V3](./회원,%20인증,%20인가.md)

| 요구사항 | 내용 |
|---|---|
| FR-MEM-01, BR-MEM-01~03, NFR-SEC-07 | 카카오 로그인·가입, state·PKCE, 서비스 세션 |
| FR-MEM-02, BR-MEM-09/12 | 로그아웃·탈퇴·탈퇴 회원 차단 |
| FR-MEM-03, BR-MEM-06/10/11 | 최초 취향 입력·기본 취향 변경 |
| FR-NOT-01~03, BR-NOT-01~06 | 인앱 알림 조회·삭제·푸시 수신 설정 |

# 목표가 아닌 것

- 구글 로그인
- 여러 소셜 계정 연결
- 이메일·비밀번호 가입
- 프로필 직접 수정
- 로그인마다 공급자 프로필 재동기화
- 언어 변경
- 서비스 access/refresh token 발급과 별도 갱신 API
- 관리자 권한
- 기기별 세션 관리
- 약관 동의 이력 저장
- 유료 상품·주문·결제·환불 연동
- 평가·랭킹·공유 기능의 회원 도메인 연동
- Redis
- Outbox
- 푸시 공급자 신규 도입

# 설계 및 기술 자료

## 공통 정책

- 카카오만 지원한다. provider 경로는 유지하고 KAKAO 이외의 값은 거절한다.
- 로그인 전 검증 정보는 HttpSession, 로그인 후 서비스 세션은 auth_sessions에 저장한다.
- 신규 회원은 ONBOARDING으로 생성하고, 유효한 취향을 처음 저장하면 ACTIVE로 바꾼다.
- ONBOARDING도 내 정보·취향·설정·로그아웃·탈퇴 API를 사용할 수 있다.
- 사용자 ID는 요청 Body가 아니라 검증된 세션에서 가져온다.
- state·PKCE·CSRF·HTTPS 검증은 생략하지 않는다.

### 공통 요청과 응답 규약

- API prefix는 /api/v1이다.
- 기존 API ID와 경로를 유지한다.
- 성공은 {message, data}, 오류는 {message, data:null, error:{code, details, trace_id}} 형식이다.
- BIGINT ID는 양의 십진 문자열로 전달한다.
- 시각은 DB에 UTC DATETIME(6), API에는 ISO 8601 offset 형식으로 전달한다.
- 302와 204 응답에는 JSON Body를 넣지 않는다.
- 인증 오류는 401, 소유권 오류는 403, 미존재는 404, 내부 오류는 500이다.
- 변경 요청은 CSRF를 검증한다.
- 인증 실패와 CSRF 실패가 겹칠 때 응답 순서는 실제 필터 구성으로 확인한다.
- 각 API의 응답 필드는 기존 명세를 유지한다.
- language_code는 기존 기본값 ko를 유지한다.

### 인증·인가 공통 처리

~~~text
요청 쿠키 KGB_SESSION 확인
쿠키 원문을 SHA-256으로 해시
auth_sessions 조회
  없거나, 폐기됐거나, expires_at <= 현재 시각 또는 last_used_at(없으면 created_at)+30분 <= 현재 시각이면 401
members 조회
  없거나 deleted_at이 있으면 401
principal에 memberId, status, sessionRowId 설정
API 접근 권한과 대상 데이터의 회원 관계 확인
업무 처리
~~~

- 인증 DB 조회 장애는 세션 만료와 구분해 500으로 처리한다. 
- DB를 읽지 못했다고 인증을 통과시키지 않는다. 
- 상태 변경 시에는 트랜잭션 안에서 회원의 삭제 여부를 다시 확인한다.

## 데이터베이스 스키마(ERD)

### 주요 테이블과 임시 저장소

| 저장 대상 | 사용 내용 |
|---|---|
| members | KAKAO 식별자, 가입 시 프로필, status, push_enabled, deleted_at |
| auth_sessions | member_id, session_id_hash, expires_at, revoked_at, last_used_at |
| member_preferences | member_id, preference_type, preference_code |
| notifications | recipient_member_id, type, title/body, reference_type/id, created_at |
| 로그인 전 HttpSession | state별 provider, verifier 원문, 고정 redirect_uri, 생성·만료 시각 |

- 회원 식별은 UNIQUE(oauth_provider, oauth_subject)로 보호한다.
- 세션 ID는 원문 대신 SHA-256 해시를 BINARY(32)에 저장한다.
- 취향 선택은 UNIQUE(member_id, preference_type, preference_code)로 중복을 막는다.
- 취향 옵션은 Enum으로 관리한다. 
- DB에는 회원이 선택한 결과만 저장한다.
- language_code는 기존 기본값을 유지한다.
- nickname은 최대 50자, profile_image_url은 최대 2048자 또는 null이다.
- email은 nullable VARCHAR(254), 없으면 FE ‘이메일 정보 없음’. 닉네임 누락은 사용자, 초과는 첫 50 code point, 이미지 URL 부적합/초과는 null이다.

### 예상 구현 구성

| 구성 | 역할 |
|---|---|
| OAuthAttemptStore | 로그인 시도 저장·만료·일회 소비 |
| OAuthMemberService | 카카오 계정 확인과 회원 가입 |
| SessionAuthenticationFilter / ServiceSessionService | 서비스 세션 검증·발급·폐기 |
| MemberService / PreferenceService | 회원·취향 처리 |
| NotificationService | 알림 조회·삭제·생성 |
| SecurityFilterChain | 공개 경로, 인증·인가, CSRF, 오류 처리 |

- Spring oauth2Login의 확장 지점을 사용한다.
- 로그인 시도 개수 제한·원자 소비·기존 auth_sessions 연결은 별도 구현이 필요하다.

## API 명세

| API ID | 기능 | Method | URL |
|---|---|---|---|
| API-MEM-01 | 소셜 로그인 시작 | GET | /auth/oauth/authorize/{provider} |
| API-MEM-15 | 콜백·회원 가입 | GET | /auth/oauth/callback/{provider} |
| API-MEM-03 | 로그아웃 | POST | /auth/logout |
| API-MEM-04 | 내 회원 조회 | GET | /members/me |
| API-MEM-06 | 회원 탈퇴 | DELETE | /members/me |
| API-MEM-07 | 취향 옵션 조회 | GET | /preference-options |
| API-MEM-08 | 현재 취향 조회 | GET | /members/me/preferences |
| API-MEM-09 | 취향 전체 저장 | PUT | /members/me/preferences |
| API-MEM-10 | 설정 조회 | GET | /members/me/settings |
| API-MEM-11 | 푸시 수신 설정 변경 | PATCH | /members/me/settings |
| API-MEM-12 | 정책 목록 | GET | /policies |
| API-MEM-13 | 정책 상세 | GET | /policies/{policy_type} |
| API-NOT-01 | 미읽음 알림 목록 | GET | /notifications |
| API-NOT-02 | 알림 하나 읽기·삭제 | DELETE | /notifications/{notification_id} |
| API-NOT-03 | 전체 알림 삭제 | DELETE | /notifications |

### 소셜 로그인 시작 API

- **API 명세:** GET /auth/oauth/authorize/{provider} (API-MEM-01)
- **권한:** 공개
- **구현 상세**
  - **요청:** Path provider=KAKAO. Query/Body 없음.
  - **처리 로직:**
    1. provider가 KAKAO인지 검사한다.
    2. state와 PKCE verifier를 만들고 challenge=S256(verifier)를 계산한다.
    3. HttpSession에 브라우저별 로그인 시도를 저장한다. 10분 후 만료되며 연장하지 않는다.
    4. 동시에 보관하는 시도는 최대 5개다. 초과하면 가장 오래된 시도를 지운다.
    5. 등록된 카카오 인증 URL로 302 이동한다. Cache-Control은 no-store로 설정한다.
- **트랜잭션:** 업무 DB 트랜잭션은 없다. 임시 시도 저장·개수 제한은 원자적으로 처리한다.
- **응답 상태**
  - 302: 카카오 로그인 화면
  - 400 COMMON_VALIDATION_ERROR: KAKAO 이외 provider
  - 500 INTERNAL_SERVER_ERROR: 내부 오류

### 콜백·회원 가입 API

- **API 명세:** GET /auth/oauth/callback/{provider} (API-MEM-15)
- **권한:** 공개 경로지만 로그인 시도 검증 필요
- **구현 상세**
  - **요청:** Path provider=KAKAO. 성공 Query code/state, 취소 Query error/state. Body 없음.
  - **처리 로직:**
    1. 브라우저·provider·state·만료를 확인하고 시도를 원자적으로 한 번 소비한다.
    2. 저장한 verifier로 카카오에 코드를 교환한다. 받은 토큰으로 사용자 정보 id를 확인한다.
    3. (KAKAO, 사용자 id)로 회원을 찾는다. 신규이면 ONBOARDING으로 만들고 닉네임·이메일·이미지를 저장하며 같은 TX에서 지갑과 가입월 무료 3회를 지급한다.
    4. 기존 회원의 프로필 재동기화는 하지 않는다. 취향·설정도 유지한다.
    5. 새 서비스 세션 ID를 만들고 해시와 만료 시각을 auth_sessions에 저장한다.
    6. 커밋 후 KGB_SESSION 쿠키를 발급하고 고정 FE /auth/complete로 302 이동한다.
    7. FE는 /members/me를 조회해 ONBOARDING이면 PREF-01, ACTIVE이면 MAP-01로 이동한다.
- **트랜잭션:** 카카오 호출은 DB TX 밖, 신규 회원·지갑·가입월 FREE_GRANT·세션은 하나의 짧은 TX다.
- **응답 상태**
  - 302: 성공 시 /auth/complete, 실패 시 /auth/error?code=허용값

| 콜백 실패 code | 처리 |
|---|---|
| OAUTH_INVALID_REQUEST | state 누락·만료·재사용·브라우저 불일치. 새 로그인 시작 |
| OAUTH_ACCESS_DENIED | 검증된 카카오 취소 응답. 로그인 화면 복귀 |
| OAUTH_AUTHENTICATION_FAILED | 코드 교환·verifier 검증 실패 |
| OAUTH_PROVIDER_UNAVAILABLE | 카카오 타임아웃·통신 장애. 잠시 후 재시작 |
| OAUTH_INTERNAL_ERROR | 회원·세션 저장 실패. 트랜잭션 롤백 |

- 신규 가입이 겹치면 DB UNIQUE로 중복을 막는다. 
- 실패한 트랜잭션을 끝내고 검증된 사용자 정보로 회원을 다시 조회하는 방식은 구현 시 시험한다.
- 같은 인가 코드를 무조건 재교환하지 않는다.
- 로그인 전 HttpSession의 인증 정보만으로 업무 API를 통과시키지 않는다.
- 서비스 세션 검증이 필요하다. 
- 커밋 후 응답이 유실되면 DB를 되돌릴 수 없으므로 새 로그인으로 복구하고 전달되지 못한 세션은 만료 처리한다.

**로그인 화면의 성공·실패 처리**

- 확인 중에는 로그인 버튼을 숨기고 로딩을 표시한다. 
- 서비스 세션 저장과 `/members/me` 확인이 끝난 뒤 이동한다. 
- 로딩 화면 자체를 위한 DB 테이블이나 API는 만들지 않는다.
- ACTIVE는 MAP-01, ONBOARDING은 PREF-01로 이동한다. 
- 이미 가입했어도 취향 저장 전 종료한 회원은 PREF-01로 보낸다.
- 인증 취소: ‘사용자에 의해 로그인이 취소되었습니다.’
- 인증 후 로그인 정보를 확인하지 못함: ‘로그인 정보를 확인하지 못했어요. 다시 시도해 주세요.’
- 네트워크·서버 오류: ‘일시적인 오류가 발생했어요. 잠시 후 다시 시도해 주세요.’
- 실패 안내는 약 2초 표시하고 AUTH-01 초기 화면으로 돌아가 버튼을 다시 활성화한다. 서버 오류 원문은 화면에 노출하지 않는다.

### 로그아웃 API

- **API 명세:** POST /auth/logout (API-MEM-03)
- **권한:** 세션 + CSRF
- **구현 상세**
  - **요청:** Body 없음.
  - **처리 로직:**
    1. 현재 세션 ID와 회원 ID가 일치하는 auth_sessions의 revoked_at을 기록한다.
    2. 커밋 후 동일한 이름·Path·Domain 조건으로 쿠키를 만료시킨다.
    3. 다른 서비스 세션과 카카오 계정 로그인은 종료하지 않는다.
- **트랜잭션:** 현재 세션 폐기를 커밋한 뒤 쿠키 만료 응답을 보낸다.
- **응답 상태**
  - 204: 완료
  - 401 AUTH_SESSION_REQUIRED: 세션 없음·폐기 후 재요청
  - 500 INTERNAL_SERVER_ERROR

### 회원 정보 조회 API

- **API 명세:** GET /members/me (API-MEM-04)
- **권한:** 본인 세션. ONBOARDING도 허용
- **구현 상세**
  - **요청:** Query/Body 없음.
  - **처리 로직:**
    1. 삭제되지 않은 본인 회원과 미읽음 알림 수를 조회한다.
    2. member_id, nickname, email, profile_image_url, language_code, status, unread_count를 반환한다.
    3. 화면 분기는 status를 사용한다. 별도 onboarding_required 필드를 추가하지 않는다.
- **트랜잭션:** 회원·알림 수를 같은 읽기 스냅샷에서 조회한다. readOnly 설정만으로 스냅샷이 보장되는 것은 아니므로 격리 수준도 확인한다.
- **응답 상태**
  - 200 member_get_success
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 회원 탈퇴 API

- **API 명세:** DELETE /members/me (API-MEM-06)
- **권한:** 본인 세션 + CSRF
- **구현 상세**
  - **요청:** Body 없음. 확인 모달은 FE에서 처리한다.
  - **처리 로직:**
    1. 회원 행을 잠그고 미삭제 여부를 다시 확인한다.
    2. deleted_at을 기록하고 oauth_subject를 충돌하지 않는 폐기용 값으로 바꾼다.
    3. 회원의 모든 서비스 세션을 폐기한다.
    4. 회원→지갑→작업 순서로 잠그고 진행 작업을 CANCELED 확정·lease 무효화한다. 남은 생성권은 REVOKE 원장과 함께 0으로 만든다. 모두 같은 탈퇴 TX에 참여한다.
    5. 커밋 후 현재 쿠키를 만료시킨다. 늦게 도착한 AI 결과가 반영되지 않도록 생성 담당과 연결한다.
- **트랜잭션:** 회원 삭제 표시·소셜 식별자 해제·전체 세션 폐기를 함께 처리한다. 외부 AI 호출은 트랜잭션 밖에서 한다.
- **응답 상태**
  - 204: 완료
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR
- **추가 설명**
  - 탈퇴한 소셜 계정으로 재가입하면 새 회원을 만든다.
  - 기존 서비스 데이터는 현재 공통 정책에 따라 30일 후 삭제·비식별화한다. 
  - 무료 생성권 원장과 가이드북 참조를 고려해 정리 순서를 정하며 단순 cascade로 모두 지우지 않는다.

### 취향 옵션 조회 API

- **API 명세:** GET /preference-options (API-MEM-07)
- **권한:** 세션. ONBOARDING도 허용
- **구현 상세**
  - **요청:** Body 없음.
  - **처리 로직:**
    1. 승인된 Enum에서 THEME, DETAIL, TRAVEL_STYLE 옵션을 가져온다.
    2. code, label, parent_code, sort_order를 구성해 정렬한다.
    3. 기본 언어로 items를 반환한다. 예시 코드를 전체 옵션 목록으로 사용하지 않는다.
- **트랜잭션:** 옵션 registry 조회에는 업무 DB 트랜잭션이 필요 없다.
- **응답 상태**
  - 200 preference_option_list_success
  - 400 COMMON_VALIDATION_ERROR: 입력 제한 위반(공통 검증 적용, FE 합의 필요)
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 현재 취향 조회 API

- **API 명세:** GET /members/me/preferences (API-MEM-08)
- **권한:** 본인 세션
- **구현 상세**
  - **요청:** Query/Body 없음.
  - **처리 로직:**
    1. member_id로 member_preferences를 조회한다.
    2. preference_type과 preference_code를 selections 배열로 반환한다.
    3. 선택한 값이 없으면 빈 배열을 반환한다.
- **트랜잭션:** 조회 전용 트랜잭션으로 처리한다.
- **응답 상태**
  - 200 preference_get_success
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 취향 전체 저장 API

- **API 명세:** PUT /members/me/preferences (API-MEM-09)
- **권한:** 본인 세션 + CSRF. 최초 입력 허용
- **구현 상세**
  - **요청:** Body selections: [{ "preference_type": "THEME", "preference_code": "HEALING" }]. 구조 예시이며 전체 유효 선택을 뜻하지 않는다.
  - **처리 로직:**
    1. 배열·필수 필드·타입·문자열 길이를 검사한다. preference_code는 최대 50자다.
    2. 같은 유형·코드의 중복을 거절한다. 422 PREFERENCE_INVALID로 처리한다.
    3. 허용 Enum과 유형을 확인한다. THEME는 1-3개, 선택한 THEME마다 DETAIL은 1-3개여야 한다. DETAIL은 선택한 부모 THEME가 있어야 한다. TRAVEL_STYLE은 선택 사항이며 8개 허용 코드 중 0~8개, 상호 배타 조건은 없다.
    4. 회원 행을 잠그고 삭제 여부를 다시 확인한다.
    5. 요청을 전체 선택 집합으로 적용한다. 누락된 기존 선택은 지우고 새 선택은 추가한다.
    6. 처음 유효하게 저장하면 ACTIVE로 변경하고 selections와 status를 반환한다.
- **트랜잭션:** 선택 집합 교체와 ACTIVE 변경을 함께 처리한다. 일부 저장 실패 시 기존 취향이 유지되도록 롤백한다.
- **응답 상태**
  - 200 preference_update_success
  - 400 COMMON_VALIDATION_ERROR: 형식 오류
  - 401 AUTH_SESSION_REQUIRED
  - 422 PREFERENCE_INVALID: 코드·대분류 개수·대분류별 중분류 개수·부모 관계 오류
  - 500 INTERNAL_SERVER_ERROR
- **추가 설명**
  - 요청에 부모 없는 중분류가 들어오면 조용히 삭제하지 않고 거절한다.
  - 생성 요청이 동시에 들어오면 중간 취향 집합을 읽지 않도록 생성 담당과 조회·잠금 경계를 맞춘다. 
  - [V1 취향 Enum 코드표](../V1%20취향%20Enum%20코드표.md)의 안정 코드와 부모 관계를 사용한다.

| 분류 | 라벨 예시 |
|---|---|
| 대분류 | 힐링, 액티비티, 미식, 문화, 자연, 쇼핑 |
| 힐링 중분류 | 조용한 곳, 온천·스파, 카페에서 쉬기, 숲·산책로, 바다 보기 |
| 여행 스타일 | 사람 많은 곳 피하기, 많이 걸어도 좋아요, 차로 이동, 아침형, 여유롭게, 아이 동반 |

### 설정 조회 API

- **API 명세:** GET /members/me/settings (API-MEM-10)
- **권한:** 본인 세션
- **구현 상세**
  - **요청:** Query/Body 없음.
  - **처리 로직:**
    1. 회원의 push_enabled를 조회한다.
    2. 기존 응답 구조인 language_code, push_enabled를 유지한다.
- **트랜잭션:** 조회 전용 트랜잭션으로 처리한다.
- **응답 상태**
  - 200 member_setting_get_success
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 푸시 수신 설정 변경 API

- **API 명세:** PATCH /members/me/settings (API-MEM-11)
- **권한:** 본인 세션 + CSRF
- **구현 상세**
  - **요청:** Body { "push_enabled": false }. Boolean 필수, null 불가.
  - **처리 로직:**
    1. 빈 객체·null·Boolean이 아닌 입력과 미지원 필드를 거절한다.
    2. 회원 행을 잠그고 삭제 여부를 확인한다.
    3. push_enabled를 전달된 값으로 변경한다. false도 유효한 값이다.
    4. language_code, push_enabled를 반환한다. false여도 인앱 알림은 저장할 수 있다.
- **트랜잭션:** 회원 확인과 푸시 설정 변경을 함께 처리한다.
- **응답 상태**
  - 200 member_setting_update_success
  - 400 COMMON_VALIDATION_ERROR: 입력 오류, 미지원 필드
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 정책 목록 API

- **API 명세:** GET /policies (API-MEM-12)
- **권한:** 공개
- **구현 상세**
  - **요청:** Body 없음.
  - **처리 로직:**
    1. 정적 정책 목록에서 policy_type과 title을 조회한다.
    2. items로 반환한다. 회원별 동의 상태는 저장하지 않는다.
- **트랜잭션:** 정적 콘텐츠 조회로 업무 DB 트랜잭션 없음.
- **응답 상태**
  - 200 policy_list_success
  - 500 INTERNAL_SERVER_ERROR

### 정책 상세 API

- **API 명세:** GET /policies/{policy_type} (API-MEM-13)
- **권한:** 공개
- **구현 상세**
  - **요청:** Path policy_type, Body 없음.
  - **처리 로직:**
    1. 등록된 정책 코드로 본문을 찾는다. Path를 파일 경로에 직접 붙이지 않는다.
    2. policy_type, title, format=MARKDOWN, content를 반환한다.
- **트랜잭션:** 정적 콘텐츠 조회로 업무 DB 트랜잭션 없음.
- **응답 상태**
  - 200 policy_get_success
  - 404 RESOURCE_NOT_FOUND: 정책 없음
  - 500 INTERNAL_SERVER_ERROR

### 미읽음 알림 목록 API
- **API 명세:** GET /notifications (API-NOT-01)
- **권한:** 본인 세션
- **구현 상세**
  - **요청:** Query page 기본1·양의 정수, size 기본4·1~20. cursor 미지원. Body 없음.
  - **처리 로직:**
    1. page와 size를 검사한다.
    2. recipient_member_id가 본인인 알림만 조회한다.
    3. created_at DESC, id DESC로 정렬하고 OFFSET (page-1)*size LIMIT size로 읽는다.
    4. items, page, size, total_items, total_pages, unread_count를 반환한다. total_items=unread_count, 빈 목록 total_pages=0이다. 조회만으로 삭제하지 않는다.
- **트랜잭션:** 목록과 전체 미읽음 개수를 같은 읽기 스냅샷에서 조회한다.
- **응답 상태**
  - 200 notification_list_success
  - 400 COMMON_VALIDATION_ERROR: 공통 입력 검증
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 알림 하나 읽기·삭제 API

- **API 명세:** DELETE /notifications/{notification_id} (API-NOT-02)
- **권한:** 본인 세션 + CSRF
- **구현 상세**
  - **요청:** Path notification_id 양의 ID 문자열, Body 없음.
  - **처리 로직:**
    1. id와 recipient_member_id를 함께 조건으로 DELETE한다.
    2. 본인 범위에 이미 없으면 204로 끝낸다. 타인 알림은 삭제하지 않는다.
- **트랜잭션:** 조건부 DELETE 후 커밋한다.
- **응답 상태**
  - 204: 삭제 또는 본인 범위에 대상 없음
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

### 전체 알림 삭제 API

- **API 명세:** DELETE /notifications (API-NOT-03)
- **권한:** 본인 세션 + CSRF
- **구현 상세**
  - **요청:** Body 없음.
  - **처리 로직:**
    1. 첫 DB 조회 시점에 존재하는 본인 알림 ID를 확보한다. 최대 20개다.
    2. 확보한 ID와 본인 회원 조건으로만 삭제한다.
    3. 그 이후 생성된 알림은 유지한다. 요청 시작을 첫 DB 조회로 해석하는 시점은 FE와 맞춘다.
- **트랜잭션:** 확보한 ID 집합만 삭제한다. 회원의 모든 행을 조건 없이 다시 삭제하지 않는다.
- **응답 상태**
  - 204: 완료 또는 빈 목록
  - 401 AUTH_SESSION_REQUIRED
  - 500 INTERNAL_SERVER_ERROR

## 비동기 처리 설계

### 내부 알림 생성

1. 가이드북 원본 업무가 커밋된 뒤 알림 생성을 호출한다.
2. 별도 트랜잭션에서 수신 회원의 삭제 여부와 알림 필드를 확인한다.
3. 알림을 저장하고 20개를 넘으면 오래된 순서(created_at, id)로 지운다.
4. 알림 실패로 원본 가이드북 생성을 롤백하지 않는다.

- 회원별 생성·정리는 같은 회원 잠금 규칙을 사용하도록 맞춘다.
- 커밋 후 호출이라는 이유만으로 별도 스레드에서 실행되는 것은 아니다.
- Outbox를 두지 않으므로 서버 종료 시 알림 누락 복구는 보장하지 않는다.
- 커밋 여부가 불명확한 실패를 무조건 재시도하면 중복될 수 있어 재시도 범위는 합의가 필요하다. 
- 푸시 설정 저장과 실제 기기 푸시 발송은 별개다.

### Notification 발생 이벤트 예시

| 알림 제목 예시 | 전달할 참조 | 이동 대상 |
|---|---|---|
| 여행 일정이 곧 시작돼요 | 가이드북 ID | GUIDE-06 |
| 가이드북 생성이 완료됐어요 | 가이드북 ID | GUIDE-06-1 |
| 가이드북의 행사 정보가 변경됐어요 | 행사 ID | INFO-02 |

- 처음에는 최신 미읽음 4개를 보여주며 최대 20개, 5페이지다. 
  - 발생 시각은 API의 ISO 8601 값을 한국 시간 `YYYY/MM/DD HH:mm`으로 표시한다.
- 클릭한 알림의 참조 정보를 보관하고 읽기·삭제 API 성공 후 목록에서 제거하고 이동한다.
  - 이동 대상 API는 해당 자원의 권한과 존재 여부를 다시 검사한다. 
  - 참조만으로 접근을 허용하지 않는다.
- 읽기 실패는 목록을 유지하고 ‘읽음 처리에 실패했습니다. 다시 시도해 주세요.’를 표시한다. 
  - 전체 읽음도 같은 규칙이며 알림이 없으면 버튼을 비활성화한다.
- 빈 목록은 ‘새로운 알림이 없어요’, 조회 실패는 ‘알림 조회에 실패했어요’와 ‘페이지에 다시 접속해주세요’를 표시한다. 
  - 실패를 빈 목록으로 처리하지 않는다.
- 상세 화면에서 돌아오면 읽은 항목을 다시 표시하지 않는다. 
  - 목록의 뒤로가기는 MYPAGE-01이다.
- GUIDE-06-1은 확인한 가이드북 뷰어 정의 GUIDE-06과 ID가 다르다. 
  - 동일 화면인지 확인 전 임의 매핑하지 않는다. 
  - 일정 시작 알림의 선행 시간과 행사 변경 판단 필드도 별도로 정해야 한다.

### 설정·정책 화면의 FE 처리

- SETTING-01의 ‘여행 일정 알림’ 토글은 계정의 push_enabled를 읽는다. 
  - 변경 실패 시 이전 값으로 되돌리고 오류를 안내한다. 
  - false여도 인앱 알림·토스트는 표시한다.
- OS 알림 권한과 계정 설정은 별개다. 
  - push_enabled=true만으로 OS 권한이 허용됐다고 판단하지 않는다. 
  - MAP-01 권한 요청과 설정 화면의 상태를 함께 맞춘다.
- AUTH-01의 이용약관·개인정보 처리방침은 모달로 표시하고 확인 버튼 또는 바깥 영역 클릭으로 닫는다. 
  - 기존 정책 API를 사용할 수 있지만 별도 동의 저장 API는 만들지 않는다.

## 트랜잭션, 협업 경계

| 처리 | 함께 맞출 내용 |
|---|---|
| 가입 → 무료 생성권 | 회원 가입 TX에서 지갑·가입월 3회 지급, 월별 멱등키 공통 사용(V1-07/11) |
| 탈퇴 → 가이드북 | 진행 작업 취소 요청 저장과 늦은 AI 결과 차단 |
| 취향 → 가이드북 | 취향 변경 중에도 온전한 선택 집합을 생성 입력으로 저장 |
| 가이드북 → 알림 | 커밋 후 알림 생성·타입·문구·이동 대상 |

## 보안 및 운영

### 인증과 권한

- 서비스 요청은 DB 세션으로 확인한다.
- 별도 access/refresh token은 발급하지 않는다.
- 역할은 일반 회원 하나다. 
- ONBOARDING/ACTIVE는 역할이 아니라 회원 상태다.
- /auth 전체를 공개하지 않고 로그인 시작·콜백과 정책 조회만 필요한 메서드로 공개한다.
- 알림은 수신 회원 ID, 가이드북은 활성 보관 관계 등 실제 테이블 관계로 접근 권한을 확인한다.
- 로그인 성공 후 새 서비스 세션 ID를 발급한다. 
- 임시 세션 ID를 재사용하지 않는다.

### 세션과 쿠키

- 로그인 시도는 10분, 최대 5개, 한 번만 소비한다.
- 서비스 세션은 절대 8시간·유휴 30분, 연장 없음, 최대 3개. 오래 발급한 세션부터 폐기한다.
- 로그인 시도 TTL을 그대로 쓰지 않는다.
- KGB_SESSION은 운영에서 HttpOnly, Secure, SameSite=Lax, Path=/, Domain 미지정으로 설정한다.
- expires_at과 revoked_at은 서버에서 검사한다. 
- 쿠키가 남아 있어도 만료 세션은 거절한다.
- last_used_at 기록은 세션 연장과 별개다. 
- 발급 시 last_used_at 초기화, 성공 인증마다 갱신. 만료/폐기 후 30일 경과 행은 매일 정리한다. 상세 V1-03 참조.

### CSRF와 배포

- 변경 요청에 Spring Security CSRF 검증을 적용한다.
- PKCE/state는 일반 API의 CSRF 보호를 대신하지 않는다.
- FE·BE origin이 다르면 허용 origin을 명시하고 credentials를 맞춘다. 
- Preflight는 인증보다 먼저 처리한다.
- 운영은 HTTPS를 사용한다. 프록시 설정·TLS 종료 지점·HSTS는 배포 환경에 맞춰 확인한다.
- 로그에 세션 원문, 인가 코드, state, verifier, 공급자 토큰을 남기지 않는다.
- client secret은 소스 코드나 FE에 넣지 않는다.
- 여러 서버를 사용하면 로그인 전 HttpSession도 공유해야 한다. 
- 공용 MySQL 업무 세션만으로 해결되지 않는다.
- 카카오 PKCE 연동이 실패하면 plain이나 검증 생략으로 낮추지 않고 원인을 확인한다.

## 테스트 항목

- KAKAO 로그인 시작 성공, 그 외 provider 거절.
- state 누락·변조·만료·재사용, 잘못된 verifier, 카카오 취소·장애.
- 같은 콜백이 동시에 와도 한 번만 소비·교환.
- 여섯 번째 로그인 시도에서 가장 오래된 시도만 제거.
- 신규 가입 경합에서 회원 중복 방지, DB 실패 시 성공 쿠키 미발급.
- ONBOARDING의 취향 입력 허용, 저장 성공 후 ACTIVE 전환.
- 취향 0개·4개, 잘못된 부모 관계·코드·중복 입력 거절.
- 각 대분류의 중분류 0개·4개 거절, 1개·3개 허용, 스타일 미선택 허용.
- PREF-02 진입 경로별 복귀, 변경 없는 저장 비활성화, 취소 시 미저장 입력 폐기.
- 로그인 도중 복귀·취소·실패 후 버튼 복구, 기존 ONBOARDING 회원의 PREF-01 이동.
- 프로필 누락 대체 표시와 401 구분, 정책 모달 열기·닫기.
- 탈퇴 확인 문구 불일치 시 버튼 비활성화, 실패·응답 유실 시 상태 재확인.
- 취향 저장 일부 실패 시 기존 값 유지.
- language_code 변경 등 정해지지 않  입력 처리.
- push_enabled=false 저장과 인앱 알림 저장이 독립적으로 동작.
- 로그아웃은 현재 세션, 탈퇴는 전체 세션 폐기.
- 동일 카카오 계정 재가입 시 기존 회원 미복구.
- 세션 만료 경계, CSRF 누락, 타인 데이터 접근, 인증 DB 장애.
- 알림 조회 시 미삭제, 삭제 재요청 204, 전체 삭제 이후 새 알림 유지.
- 동시 알림 생성 후 회원별 최대 20개 유지.
- 탈퇴와 가이드북 생성 완료가 겹쳐도 결과 반영 차단.

검증 로직은 단위 테스트로, UNIQUE·잠금·롤백은 실제 MySQL 통합 테스트로 확인한다.  
OAuth·쿠키·CORS는 브라우저 연동 테스트가 필요하다.

## 추가 결정 필요 사항

확정된 세션·이메일·취향·무료 지급·번호 페이지·잠금 규칙은 다시 미정으로 돌리지 않는다. 아래 실연동 항목만 남긴다.

- 카카오 콘솔 값, CSRF 전달/재발급/필터 우선순위, 실제 origin 및 다중 서버 임시 세션 저장
- FE/AI의 취향 코드·라벨 매핑 검증
- 알림 목적지 GUIDE-06-1, 일정 시작 선행 시간, 행사 변경 조건, 재시도/중복 처리 및 실제 푸시 범위
- 정책 본문·백업 보관/복원 후 삭제 재적용·탈퇴 정리 배치 검증

### 셀프 리뷰

- [ ] 서비스 세션 TTL과 OAuth 로그인 시도 TTL을 구분했다.
- [ ] 인증·CSRF·소유권 검증을 생략하지 않았다.
- [ ] 회원·취향 저장 실패와 탈퇴 경합을 검증했다.


## 2026-09-15 V1 적용 기준

[개발 전 확정 필수 내용](../개발%20전%20확정%20필수%20내용.md)의 V1-03~17을 구현 계약으로 적용한다. 세션 절대 8시간·유휴 30분·최대 3개, nullable email, 취향 코드표, 가입월 포함 월 3회 누적 이월, 동행/기간 제한, 알림 page, 태그 제거, DB lease 및 회원→지갑→작업→가이드북 잠금 순서가 반영됐다. 외부 AI·카카오 설정·CSRF 전달·TourAPI·HTML/PDF는 각각의 연동 관문이며 구현 완료로 간주하지 않는다.
